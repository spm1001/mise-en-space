# Handoff — 2026-04-08

session_id: 8b9c085b-27a9-437c-aa34-90710c15e2f3
purpose: Close mise-vakabu (pageless + merged cells), then sweep standalone items
format: fond-v1

## For the next Claude

### Done
- **mise-vakabu** (closed) — all 6 actions complete. Pageless doc creation verified end-to-end. Merged-cell integration tests across 3 patterns (horizontal headers, vertical categories, mixed merges + formulas). 4 integration tests for sheets, 1 for pageless.
- **mise-benega** (closed) — `do(create, doc_type='folder')` with `supportsAllDrives=true`. Early return in `do_create` before content validation. 6 unit tests.
- **mise-mujuho** (closed) — Token error messages now distinguish missing file, corrupt JSON, no refresh_token, and failed refresh. `_load_and_diagnose_credentials` helper in `adapters/http_client.py`. 4 unit tests.
- **mise-gonase** (closed) — Indented headings no longer get blockquote prefixes. One-line fix in `extractors/docs.py`: `and not heading_prefix` added to blockquote condition. 1 unit test.
- **MCP property drop bug** — discovered and fixed. Tool descriptions exceeding CC's 2048-char `MAX_MCP_DESCRIPTION_LENGTH` cause the Anthropic API to silently drop properties from the schema during `tool_reference` expansion. `_DO_DESCRIPTION_FULL` shortened from 2494→~600ch. Filed cross-repo field report (bds-kusodu).

### Reflection
- The MCP property drop investigation was the most valuable unexpected work. What started as "verify pageless works" became a genuine bug hunt across three codebases (mise, claude-code, the Anthropic API). The key insight: description truncation at the CC client level (`client.ts:218`) somehow triggers property loss during the API's server-side `tool_reference` expansion. Shortening descriptions below 2048ch fixed it — all 21 `do()` properties and all 5 `fetch()` properties now survive.
- The three standalone items (benega, mujuho, gonase) were clean because the earlier vakabu work had loaded the right context: server.py dispatch, tools/create.py, http_client.py, extractors/docs.py.

### Risks
- Test spreadsheet `16WZmekuVlUVl1gHUHHexBhtjKdLTu6f4mynsHW3jvgs` in Drive is a dependency for merged-cell integration tests. If deleted, tests fail with API error instead of clean skip.
- The description length fix is empirical — we proved shortening works but don't know the exact API-side mechanism. If descriptions creep back over 2048ch (new params, new operations), properties will silently drop again. The `docs/decisions.md` entry warns about this.

### Opportunities
- mise-zirupo remote path (tokiju → winala → sefepo) is the big remaining outcome
- mise-putadu (Gmail pagination) and mise-heferu (image/PDF edges) are independent tracks
- The CC field report (bds-kusodu) needs someone to actually file the GitHub issue and audit other batterie MCP servers

## For Claudes to come

MCP tool descriptions have a hard ceiling at 2048 characters, enforced by Claude Code's `MAX_MCP_DESCRIPTION_LENGTH` constant at `src/services/mcp/client.ts:218`. Descriptions over this limit are sliced and suffixed with `'… [truncated]'`. This truncation causes a secondary, more insidious failure: the Anthropic API silently drops properties from the tool's JSON schema during `tool_reference` expansion (the mechanism that expands deferred tools when ToolSearch is called). The schema is sent in full by CC — the property loss happens API-side during expansion, not during transmission.

The diagnostic signature: ToolSearch returns fewer properties than `mcp._tool_manager._tools[name].parameters` reports. The dropped property appears nowhere in the ToolSearch result — not truncated, just absent. When Claude emits a tool_use including the missing property, the API strips it before CC sees the tool_use block, so CC's `z.object({}).passthrough()` (which would preserve unknown properties) never gets the chance to help.

The fix within MCP server control: keep tool descriptions short and put detail in MCP resources (`mise://tools/*`). The `do()` description went from a full Args/Returns docstring (2494ch) to a compact operation summary (~600ch). All 21 properties survived immediately. Don't expand descriptions without checking `len(tool.description) + len(json.dumps(tool.parameters))` against practical limits.
