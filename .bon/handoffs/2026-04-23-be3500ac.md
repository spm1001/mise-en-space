# Handoff — 2026-04-23

session_id: be3500ac-52f1-428a-8ab2-8e11ac491f73
purpose: Read the Gemini Enterprise Agent Platform announcement past the marketing, map it to MIT's actual prior art at ITV, and form a POV on unification — including whether Mise can become an org-wide MCP server.
format: fond-v1

## For the next Claude

### Done
- Walked the Gemini Enterprise Agent Platform announcement (April 2026) and its companion "what's new in Gemini Enterprise" post via `passe fetch`. Pulled the editions table from the JS-blocked pricing page via curl + JSON-escape parsing. Confirmed: Vertex AI is being absorbed into "Agent Platform"; the Gemini Enterprise *app* is the user-distribution surface; ADK-built agents need at least the Standard edition ($30/seat/month) to be importable into the app — Business ($21) is no-code-only. Plus is contact-sales with sovereign data boundaries.
- Discovered ITV's actual prior art is much richer than expected. **The big one Sameer hadn't flagged:** ITV already runs an internal AI Agent Hub (`ITV/ai-hub-management`) on Open WebUI + LiteLLM, with YAML-driven config for groups/users/models/functions, dev/prd separation, GitHub Actions for self-service. Production agents already wired in via "function" pipelines: `itv_sales_agent`, `itvx_user_activity_agent`, plus media tools (`gemini_video_understanding`, `nano_banana`, `runwayml`, `veo3`, `archibot`, `itv_insights`).
- Mapped the ADK landscape at ITV: Content Platform team has standardised on `ITV/cp-agent-skeleton` with five+ live agents (`cp-aplo-efficiency-agent`, `cp-architecture-agent`, `cpv3-builder` — uses `McpToolset` with `StdioConnectionParams`, `tf-tester-agent`). MIT side: `ITV/mit-mailbox-agent` is Rupert Coghlan's (roopzRMC) ADK + RAG agent, co-led with Alex Green; full Phase 1-5 plan in PROJECT_PLAN.md.
- Could **not** find separate "wiki agent" or "benchmark agent" by Rupert in ITV org or roopzRMC's namespace despite a thorough sweep. `mit-wiki` is a Docusaurus site (no agent code). `mit-hhl` has a benchmark schema doc but no ADK code. Open question filed as `mise-biniza`.
- Confirmed Mise is **already** FastMCP with `transport="streamable-http"` (`mise-en-space/server.py`) — i.e., remote-MCP capable. The ADK-consumes-remote-MCP pattern is already in production at ITV (`cpv3-builder`'s import: `from google.adk.tools.mcp_tool import McpToolset` + `from google.adk.tools.mcp_tool.mcp_session_manager import StdioConnectionParams`). Mise's OAuth client is in the ITV-owned `mit-workspace-mcp-server` GCP project, so any @itv.com user can consent — no procurement gate.
- Reframed `mise-zirupo` ("Web Claude and Office Claude can access Drive and Gmail via mise" → "Web Claude, Office Claude, **and ITV Agent Hub** can access Drive and Gmail via mise") with new --why/--what/--done that name the Hub as a target distribution surface alongside claude.ai.
- Filed bons:
  - `mise-kafovo` — v0 ITV pilot: shared SA + mailbox-agent McpToolset (smallest useful step before multi-tenancy)
  - `mise-suzoso` — Survey multi-tenant MCP patterns from servers we've evaluated (precedes `mise-tokiju` OAuth middleware design)
  - `mise-biniza` — Field report: locate Rupert's wiki agent and benchmark agent
  - `plg-piluwa` (in plongeur) — Re-evaluate Plongeur's Agent Builder post-mortem against the new Agent Platform
- Updated `~/.claude/context/self.md` with the "confabulating about confabulation" lesson — I labelled a subagent's `constraints/vertexai.allowedPartnerModelFeatures` block as confabulation before verifying; the block was real.

### Reflection
- The biggest win of the session was the Hub discovery, and it almost didn't happen. The user's framing was "find Rupert's two agents and Plongeur"; if I'd executed only on that, I'd have missed `ai-hub-management` entirely and given a strategic answer that ignored the platform Sameer's team will actually ship into. The lesson is that prior-art rummages should cast wider nets than the literal ask — the orienting context (what the org actually has) is often more decisive than the named artefacts.
- The pricing-tier extraction worked because curl-grep against the obfuscated JS still caught `$21` and `$30` and the editions text. When passe's quality gate fails (Chrome unreachable), don't give up — `curl | grep -oE` against the raw HTML often gets you the substance even from JS-rendered pages. Worth knowing for future research blocks.
- Couldn't find the wiki/benchmark agents and chose to be honest about it rather than pad with speculation. That felt right; the alternative would have been to call the mailbox agent "the wiki agent" and proceed on a wrong premise.
- The "confabulation about confabulation" moment was salutary. Same shape as the `sed -i` incident — pattern-match without verification, narrate satisfyingly, get gently corrected. Self.md updated.
- Sameer's "ah - ok - got it" pivot from strategy-doc depth to the targeted Mise-MCP question was a perfect illustration of how the partnership flows — the wide-angle answer surfaced a sharper question, and the sharper question was the actually-ship-this-week one. Don't conflate "comprehensive" with "useful."

### Risks
- **The unification POV is built on partial information.** Until Rupert confirms what his other two agents are (`mise-biniza`), the proposed shape — Plongeur tools + mailbox + wiki + benchmark all under one MIT measurement agent — is hypothesis-shaped. Don't act on the architecture until the inventory is complete.
- **The Hub team's MCP support is assumed, not verified.** ADK consuming MCP is solid (`cpv3-builder` proves it). Whether Open WebUI in ITV's specific deployment supports MCP servers as a first-class surface (vs only ADK agents being able to call MCP from the inside) is a 5-minute conversation that hasn't happened. The v0 pilot in `mise-kafovo` deliberately routes MCP through ADK to sidestep this until confirmed.
- **The Plongeur migration assumes the Agent Builder post-mortem failures are addressable in the new platform.** They might not be. `plg-piluwa` exists to test this before commitment — don't sketch tool extraction until that's been done.
- **Auth multi-tenancy (`mise-tokiju`) is the load-bearing problem for v1.** v0 deliberately punts on it via shared SA. If we ship v0 and skip the survey (`mise-suzoso`), we'll likely under-design tokiju and have to rebuild — Sameer's instinct that other MCPs have good patterns is the corrective. Do the survey first.
- **20k buffer in statusline** (from yesterday's session) is calibrated to current Opus 4.7 + CC 2.1.116 defaults. Today's session was relatively short and didn't stress the gauge — re-run `~/.claude/scripts/statusline-audit.py` if the next "shows free %, hits the wall" complaint surfaces.

### Opportunities
- **Talk to whoever owns `ITV/ai-hub-management`** about MIT onboarding. Ask specifically: (a) the PR review SLA for new functions, (b) their dev/prd lifecycle expectations, (c) whether they've thought about exposing MCP servers to Open WebUI users (vs only inside ADK agents), (d) whether they have an opinion on `cp-agent-skeleton` adoption beyond CP. This conversation likely changes the order of operations across `mise-kafovo`, `mise-tokiju`, and `plg-piluwa`.
- **Once Rupert confirms his agents** (`mise-biniza`), redo the unification sketch. Possible the right "first MIT agent in the Hub" isn't `mit-mailbox-agent` but one of his — the architecture is downstream of what's actually closest to ready.
- **`cp-agent-skeleton` adoption for MIT** is mentioned in the strategy POV as a "cheap unification win." Worth a 30-minute look at the skeleton repo to see whether MIT can fork-and-evolve, or just adopt as-is, or whether it's CP-specific in ways that make MIT's use awkward. That'll inform how much architecture work the Plongeur migration actually needs.
- **The session-historian patterns Sameer hinted at** ("other MCPs we've looked at have good multi-tenant patterns") — `mise-suzoso` is the bon, but the search starts with `garde-manger` / past CC sessions referencing MCP server evaluations. The `ce-sessions` skill or `compound-engineering:research:ce-session-historian` agent are natural starting points.

## For Claudes to come

ITV's distribution surface for AI is already chosen and built — it's not Gemini Enterprise app, it's Open WebUI + LiteLLM in front of YAML-driven config (`ITV/ai-hub-management`), with ADK agents deployed to Vertex AI Agent Engine and exposed via "function" pipelines (`config/{dev,prd}/functions/*.py`). The Content Platform team's `ITV/cp-agent-skeleton` is the canonical ADK template. MCP is consumed via ADK's `McpToolset` with `StdioConnectionParams` — proven in production by `ITV/cpv3-builder`. Two consequences worth carrying forward: (a) the Gemini Enterprise app SKU/pricing question is largely moot for distribution at ITV — the org has its own answer, free at the per-seat level; (b) new MIT work (mise as org-wide MCP, Plongeur's tools, future agents) should target the Hub as its surface, not invent a parallel one. Mise is the test case — `mise-zirupo` and its actions are the work-in-progress proof of this pattern. If a future session is reasoning about "how do we get an internal team's agent in front of the wider org at ITV," the answer is *not* "buy Gemini Enterprise app" — it's "ship a function into `ai-hub-management`, with the agent deployed to Vertex AI Agent Engine, optionally consuming MCP servers like mise via `McpToolset`."
