# Handoff — 2026-05-04

session_id: 3afa2ae4-d9cc-4525-8c0b-69170adf2609
purpose: Single-step mise install into Claude Desktop / Cowork. Closed the 4-month plugin-MCP gap, shipped in-app OAuth bootstrap, validated end-to-end with field test.
format: fond-v1

## For the next Claude

### Done

- **Closed the 4-month plugin-MCP gap.** Built `mise-cowork-plugin-v3.zip` from the current repo, uploaded via Cowork → Customize → upload custom plugin, validator accepted, runtime spawned, mise tools surfaced as `plugin:mise:mise/<tool>` (search, fetch, do). The April field-report "structural dead zone" is fixed — both the marketplace validator (now accepts inline `mcpServers` without `type`) and the runtime delivery (uploaded plugins now stage into `CLAUDE_CONFIG_DIR`, where CC's standard plugin loader walks them) cooperate. See `docs/2026-04-10-cowork-plugin-mcp-gap.md` — that report's "two halves of the system don't agree" framing is now historical, not current.
- **Shipped in-app OAuth bootstrap.** New `mise.do(operation="setup_oauth")` opens a Mac browser at the consent screen, runs a detached `python -m auth --auto` subprocess that listens on `localhost:3000`, exchanges the code, and saves the token to macOS Keychain via `save_token`. Returns immediately with the URL inline as a fallback. Five files touched: `tools/setup_oauth.py` (new), `auth.py` (added `--auto` flag), `oauth_config.py` (always create persistent dir), `tools/__init__.py` + `server.py` (dispatch wiring), `adapters/http_client.py` (friendly bootstrap hint replaces the bare-CLI message). Plus `hooks/ensure-mise.sh` updated to point at the new tool. Plugin `version: 0.6.0`.
- **Validated end-to-end with a Kate-shaped field test.** Renamed Keychain entry to simulate a fresh user; uploaded v3; opened a fresh Cowork conversation; InnerClaude saw the new hook message ("ask Claude to call mise.do(operation=\"setup_oauth\")"); offered to run it; browser opened; consent completed; token saved; mise.search returned ITV measurement results. The whole "find me docs about X" → "click consent" → "results" flow is empirically the smooth path.
- **Filed six polish items** in `.bon/quickfile-2026-05-04-cowork-polish.md` (Dolt was offline so direct `bon new` failed; quickfile is git-tracked and committed). Items: identity self-disclosure in cues; Cowork-aware SKILL.md; field report on connector disambiguation; setup_oauth unit tests; auth.py --auto port pre-check parity; Cowork plugin platform-gating investigation.
- **Synthesized the 2026-04-27 handoff's "For Claudes to come" section** into `.bon/understanding.md` at session start: added MCP Resources empirical reference + the mise-cuvusa cache vision with sequencing risks.
- **Updated CLAUDE.md OAuth section** to lead with `setup_oauth` as the canonical bootstrap path, document the Internal-mode OAuth client status (`mit-workspace-mcp-server`, any `@itv.com` user just works), and clarify token storage hierarchy (Keychain → persistent data dir → ephemeral plugin root).

### Reflection

- **The fix wasn't ours; the bootstrap UX was.** The plugin-MCP gap closed because Anthropic stopped trying to parallel-implement plugin loading inside Cowork's runtime — they now stage uploaded plugins into `CLAUDE_CONFIG_DIR` and let CC's existing machinery do the work. We didn't fix that; we just had to wait. What we did do well: design a smooth bootstrap UX layered on top, where the friendly error message points at an in-app remediation tool, the tool spawns a subprocess so the MCP call returns fast, and self-disclosing data beats skill copy.
- **I confidently misread the v2 mcpServerStatus snapshot at 4 seconds post-stage** and called the install dead before mise's stdio server had finished its handshake. Looked at one snapshot, declared failure. Two minutes later the same query showed `plugin:mise:mise` connected with 14 tools. Sameer's testimony from inside the Cowork conversation ("here's the chat with InnerClaude... it lists my personal Drive lol") was the corrective. Lesson added to self.md mid-session under "Silent successes are a class of bug" — generalises to "when a snapshot says 'absent', check if the absence is permanent or transient before naming a cause."
- **Sameer's calendar-connector observation was the most valuable in the session.** He noticed that InnerClaude's "today's calendar" answer came from Cowork's *native* Drive/Calendar connector (bound to his personal account), not from mise (bound to ITV). That's the multi-account ambiguity Kate will hit too. The polish bons captured it; the most leveraged fix is `cues._identity` on every mise response, so the data self-discloses.
- **Bon's Dolt backend dropped mid-session.** Earlier in the session `bon list` returned the full hierarchy; later `bon new` failed with "Parent not found" and `bon list` showed 0 outcomes. We routed around with the quickfile pattern. Worth investigating whether the local Mac Dolt connection is fragile or this was a one-off. (If recurring, the dolt-bon.service notes in understanding.md need updating with a Mac-side recipe.)

### Risks

- **mise-zozewa now more visible.** `setup_oauth` saves to Keychain → deletes file → next mise call's `resolve_token_path` materialises Keychain → file → save_token deletes again. The token.json on disk reappears after every API call. Cosmetic but visible to anyone watching the data dir. Already filed; the implementing Claude should be aware this got worse, not better, with the new bootstrap flow.
- **Friday demo is for one test user (Kate, ITV) on the Internal-OAuth-mode happy path.** The bootstrap flow as designed assumes: Mac with browser; localhost:3000 free; ITV Google account. If Kate has port 3000 held (Node dev server is the classic — see Bon 5 in the quickfile), `setup_oauth` returns a clean error but `auth.py --auto` would not. Friday is fine; the post-Friday rollout to other ITV folks will hit edge cases.
- **The Cowork plugin bundle has no platform gating.** Bon 6 captures this — Windows install would surface as "uv: command not found" or a stdio server that immediately exits. No Friday concern (Sameer + Kate are both Mac), but blocks broader rollout.
- **plugin.json bumped to 0.6.0; pyproject is dynamic.** The plugin's displayed version is now correct for the bundle. If we ship via marketplace, the version is what discriminates "old" from "new" for users — keep this in sync with future code changes.
- **The polish quickfile depends on bon backend connectivity to land.** If Dolt stays offline for weeks, the six items risk drift. The file is committed and discoverable, but a follow-up Claude should file properly when Dolt is reachable.

### Opportunities

- **Pick up `cues._identity` first** (Bon 1 in the quickfile). Highest leverage, smallest blast radius — every mise response self-discloses which Google account answered. After that, Kate and her colleagues can have multiple Workspace connectors loaded and Claude won't pick the wrong one silently.
- **Expand the bundle to include a subset of batterie skills** — bon's open/close, trousse subset (titans? screenshot?). The MCP path is validated; adding skills is mechanical (drop more `skills/<name>/` dirs into the staged bundle, re-zip, re-upload). But test the open/close skills' assumptions about CC-specific paths first — the workspace skill body had `/exit` then `claude` text that's nonsense in Cowork (Bon 2 captures this; same audit applies to any other skill we add).
- **Consider the field report (Bon 3) before more capability lands.** The connector-disambiguation problem is bigger than mise. Worth a docs/ writeup that tracks: which Cowork users are likely to have multiple Workspace identities (everyone with an ITV-style work + personal split), what plugin authors can do, what Anthropic should do. Could become an Anthropic-facing post.
- **The 4-month-stuck story is worth telling.** Three handoffs (2026-04-08, 2026-04-09, 2026-04-10) document failed approaches, and 2026-04-27 names what to abandon. Today's 2026-05-04 closes it. Worth a longer-form retro at some point — the failure mode that matters is "we hit a wall and called it structural; it was actually a feature being shipped."

## For Claudes to come

**Cowork is Claude Code in a VM with `~/.claude/` swapped for a session-scoped temp dir.** When a user uploads a plugin via Customize → Browse plugins → upload, Cowork stages the bundle into `CLAUDE_CONFIG_DIR=/var/folders/.../<id>` for that session. CC's standard plugin loader walks that dir, reads `.claude-plugin/plugin.json`, fires `SessionStart` hooks, launches stdio MCP servers via `mcpServers.*` — all on the **Mac side**, all unchanged from native CC behaviour. Skills, MCP, hooks, and commands all reuse CC's existing machinery. The April 2026 field report's "three separate extension systems with no shared plumbing" framing has collapsed into one. The implication for plugin authors: a single `.claude-plugin/plugin.json`-shaped bundle works for both CC and Cowork; nothing Cowork-specific is needed in the plugin itself. The implication for diagnostics: when something doesn't land, the failure is in the Mac-side spawn, not in the VM.

**The bootstrap UX pattern that lands well:** the *friendly error wrapper* pattern. When an external dependency (OAuth token) is missing, the tool's error response includes a *concrete in-app remediation pointer* — not a CLI command, but the name of another tool the same Claude can invoke. The remediation tool itself spawns a detached subprocess so the MCP call returns immediately with a URL inline (in case the user-side action — opening a browser, etc. — needs a fallback). The data self-discloses; Claude reads the error, finds the remedy verbatim, calls it. This pattern showed up twice in this session: (1) `mise.do(operation="setup_oauth")` invoked because the error message named it, and (2) the "your search returned X via mise; Y connector also has calendar" insight that became Bon 1 (`cues._identity`). The general principle: prefer self-disclosing data over instructional copy. Skill copy may not be read; data attached to a response always is.

**OAuth-client User type matters and is invisible to plugin authors.** mise's OAuth client lives in ITV's `mit-workspace-mcp-server` GCP project and is set to **User type: Internal** — meaning any `@itv.com` Workspace user can authenticate without verification or a test-user list. That's the magic mode for company-internal apps and bypasses Google's ~6-week verification process for sensitive scopes. Worth checking the consent-screen User type before assuming a Workspace MCP is broadly usable: External-test mode caps at 100 explicitly-added users; External-published shows a scary "Google hasn't verified this app" warning; Internal just works for everyone in the org. The setting lives at `console.cloud.google.com/apis/credentials/consent?project=<project_id>` and isn't queryable from `gcloud` CLI directly. For any future ITV-internal MCP, default to Internal mode if possible.
