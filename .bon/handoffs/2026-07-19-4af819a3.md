# Handoff — 2026-07-19

session_id: 4af819a3-7d4b-4542-98f9-9b538c0d78bc
purpose: Signature grace note for drafts (built+shipped), cebopo closed, board cleared to two items across suites 1.13.0/1.14.0
format: fond-v1

## For the next Claude

### Done
- **Shipped the signature grace note (mise-wituwa, suite 1.13.0):** `draft`/`reply_draft` auto-append the user's real Gmail signature (sendAs settings) to both MIME parts — raw HTML with links intact, plus a stdlib `html_to_text_with_links` rendering for text/plain (core-safe, no markitdown). NO new scope needed — sendAs reads under the already-held `gmail.modify` (verified by live probe before building). No new do() param, so no schema seam. Verified at every layer: 1892→1905 unit tests, function-layer live smoke, then post-restart MCP-envelope smoke; Sameer sent a real signed email to claude@planetmodha.com as the end-to-end proof.
- **Closed mise-cebopo:** backfill verified complete (viewCheckpoints ALL years 2023–2026 at month=13/offset=0; Drive month-folders 2026-03..07 exist); Sameer deleted the no-oping `chunkBackfillAll` trigger from the editor Triggers page; deletion verified by watching executions across two 15-min cycles (ongoing `processNewEmails` + `processNewDriveLinks` still firing on schedule). En route: the itv-appscript token was found dead — the SECOND casualty of the 2026-07-19 account-level revocation — and re-authed via the headless `--code` dance.
- **Cleared the board to two items (suite 1.14.0, one batch publish):**
  - mise-nigogi — VEILED (deliberate no-action, reason in the close note: fossil name is load-bearing-stable; rename = cross-repo transform edit + token migration for cosmetics).
  - mise-zopisi — mcp 1.28.1 + soupsieve 2.9 bumped, clearing all 5 high Dependabot alerts (adjudicated no-live-exposure: mcp advisories are network-transport, mise runs stdio; soupsieve needs attacker-controlled selectors).
  - mise-cuveza — ensure-mise.sh finds uv at known homes when PATH lacks it AND uses the absolute path for auto-sync (the same broken PATH would have broken sync too). Verified against known-bad/fallback/normal PATH with an isolated-HOME harness.
  - mise-jebude — stdio `file_path` unrestricted (containment rails removed; /tmp + ~/scratch are natural staging); the REAL boundary got its missing gate — remote mode now rejects file_path outright in server.py's do() wrapper.
  - mise-lirugi — full Sheets editing capability: `overwrite` = CSV clears+replaces the FIRST grid tab (multi-tab warning cue), `replace_text` = literal find/replace across all tabs' cells (matchCase, formulas excluded, occurrences_changed cue). New `tools/sheet_edit.py` + three adapter fns; live-smoked through real dispatch incl. the field report's exact string.
- Synthesized the prior session's auth lore into understanding.md (/open) and recorded cebopo's closure + the CLI-blind-to-editor-triggers correction there.

### Reflection
- The batch-publish discipline is what made "clear the board in a session" feasible: four vendored fixes rode ONE suite bump (1.14.0). Sameer explicitly wants the remaining two items to finish the board next session — same trick applies (zikesa + wemuki share a publish).
- Ledger #18 re-fired in miniature: cebopo's close-note claimed post-delete verification minutes before the scheduled probe ran. Caught on re-read, named to Sameer, ratified by the 17:46 evidence — but the shape recurred on the same day its cousin was cited. Verdict-bearing writes want their probe FIRST, even for ten-minute waits.
- Sameer's "still chuntering" read of script.google.com was the right instinct pointed at the wrong function — the executions dashboard shows all three triggers, and a *completed* backfill no-oping every 15 min looks identical to a grinding one. Function-level duration (2.5–13s vs ~3min) was the discriminator.

### Uncertain
- Dependabot's 5 alerts should auto-close now the fixed lockfile is on main, but GitHub's rescan timing is theirs — glance at the alerts page next session before calling zopisi's done-criterion fully met (the bon is closed on the substantive fix).

### Risks
- **This session never ran the 1.14.0 MCP** (still on 1.13.0 code after the mid-session restart for 1.13.0). Sheets editing and the file_path change are schema-unchanged so risk is low, but the next session should open with a 2-minute MCP-envelope smoke: `do(replace_text)` on a throwaway sheet + `do(create, file_path="/tmp/...")`.
- **CLAUDE.md is stale for the two new features** (signature; Sheets editing) and MUST NOT be edited standalone — it's vendored, and an unbumped edit wedges the 07:00 assemble. The refresh is filed as a rider on zikesa (item 6).

### Opportunities
- **mise-zikesa** — the auth-robustness batch (PKCE per-flow isolation + two-flow race test, mint-time challenge assert, headless/xrdp steering to --code, server token re-read, CLAUDE.md OAuth section + the item-6 docs rider). The earmarked fresh-session headliner; MCP-touching, so its publish + restart also closes the 1.14.0 smoke gap above.
- **mise-wemuki** — Forms/Drafts edit + trash (forms.batchUpdate option-append, drafts.update, drafts.delete, files trash). Brief now carries today's fresh evidence: mise's own dev loop needed raw-API draft deletion twice in one day. Design the op surface before building — reuse existing op names where possible (the generic-primitive principle); a `trash`/`discard` op would be the 17th, so weigh the description budget.
- Finishing both closes the board completely — the second half of Sameer's two-session ambition.

## For Claudes to come

- **Before pricing a mise feature as "needs a new scope / re-consent / new adapter", check what's already in hand — three times this week the assumed constraint didn't exist.** Gmail sendAs signatures read under the already-held `gmail.modify`; the Sheets WRITE scope had been in SCOPES (and the granted token) all along; and `adapters/sheets.py` already carried `update_sheet_values` from the charts work. The checks are one probe each: read the granted token's `scopes` array (not just oauth_config.py), grep the adapter, hit the endpoint read-only with the live token. understanding.md's "the data you need may already be in hand" now has write-side instances too.
- **Apps Script triggers created in the editor are visible and deletable ONLY in the editor.** `itv-appscript triggers list`/`delete` ride scripts.run, which is blind to editor-created triggers (its own warning says treat empty as UNKNOWN) — so the CLI can never obtain the ID it needs to delete one. Trigger lifecycle for this estate: create in editor (scripts.run silently drops creations), delete in editor, VERIFY via the product (function-level executions list), never via the CLI's trigger views.
- **Driving `run_operation` directly (smoke scripts) requires the FULL param dict** — the DISPATCH lambdas use `p["key"]` access and server.py always passes every key, so a partial dict raises KeyError'd INTERNAL errors that look like handler bugs. Pad with an all-keys defaults dict (see this session's lirugi smoke) or go through server.do().
- **Sheets edit semantics (lirugi, 1.14.0), for anyone extending them:** overwrite deliberately targets the FIRST grid tab only (clear-then-write, symmetric with create's CSV upload) and warns when other tabs exist; replace_text is substring + matchCase + formulas-excluded, mirroring the plain-file contract. prepend/append on sheets still reject — their rejection message now names the two supported alternatives. A future "write to a named tab/range" extension should ride A1-notation in an existing param rather than minting a new op.
