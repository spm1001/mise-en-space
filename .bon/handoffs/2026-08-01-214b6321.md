# Handoff — 2026-08-01

session_id: 214b6321-99ba-4be5-a447-7e8bd024ae2d
purpose: mise-pojoro — the week's real usage reviewed, two instruments paired; plus mise-livano shipped
format: fond-v1

## For the next Claude

### Done

- **mise-pojoro complete**: `docs/2026-08-01-usage-review.md`, 16 findings adjudicated to
  surfaces with verdicts. Method was the brief's paired-instrument design: a frozen
  calls.jsonl snapshot (what reached mise) × transcript sweep over 313 files/120MB (what
  agents were trying). Headline: **refusals that teach get obeyed in seconds; raw 404s cost
  minutes** — every teaching error on Friday was followed to the letter within ~30s; the one
  bare 404 cost a 7-minute detour. Top capability gap by pain: **Sheets range read/write**
  (~16 hand-rolled Google-API scripts in one morning, pattern recurring three days later).
- **April baseline found via git history** (Sameer's steer) after I'd wrongly declared it
  absent: 2026-04-09 stock-take handoff. Volume now 2.3× daily; do() share halved —
  mise is increasingly a research instrument.
- **Filed**: mise-tuveda (404s teach — carries the three real test ids), mise-soheka
  (guidance documents `mcp__mise__*`, harness exposes `mcp__plugin_mise_mise__*`; one
  observed no-such-tool failure; MEASURE PER SURFACE before rewriting), mise-tezudu
  (injection-surface scrub, out of the Klartext eval), mise-gilojo (Sameer's spike:
  intercept the Gmail sync call Show-original rides — cookie-only Ktbx resolution, and
  the capture yields jujoti's long-wanted thread-a matched pair either way).
- **Enriched**: mise-saroca (mechanism confirmed from the Fable transcript: mise's own
  email_context cue mints the 404ing id; adapter holds threadId at search time — also
  converted outcome→action, it's one fix), mise-lerulo (Show original's URL carries
  `permmsgid=msg-f:<decimal>`, convertible by the confirmed thread-f transform; accept
  msg-f/show-original-URL/Message-ID as fetch inputs).
- **mise-livano shipped and closed**: `html.unescape` on matched URLs in
  `adapters/gmail.py`; test watched red on the real key shape (`['disco','amp;usp','amp;tab']`)
  first; 93/93, mypy clean, CI green (de8ab1c). Step-4 sweep: it was the only URL
  harvester, so the gap is closed everywhere.
- **Coexistence with a parallel session in this same working tree held throughout**,
  including a same-window push race: the three agreed rules plus one tightening —
  the index is shared, so stage-and-commit in ONE Bash call, never leave the index loaded.
- Cross-repo: **trousse-cumoza** filed (deglacer reference gains the two dragons this
  session's wrong greps bought). CLAUDE.md Call Log section now warns the logger omits
  `base_path`.

### Reflection

The brief's method decision (two instruments, paired) was vindicated repeatedly: the log
said "silent drop, no recovery" where the transcript showed recovery by another route; the
log's 98.8% ok rate concealed the week's real costs exactly as predicted. My own instrument
errors were the same shape twice — a too-clean number (0 of 3 skills loaded, 50 of 50
rejected) that was the probe's pattern, not the world. Both caught, neither automatically:
the known-positive-control habit fired on suspicion, and should fire by default.

Sameer's soft questions did their documented work twice more: "we got permission granted?"
corrected my day-stale preview-gate claim (the bench handoff records acceptance landing
mid-session 31 Jul — tools/call is LIVE); "git history maybe?" found the April baseline
I'd declared absent.

### Uncertain

- Whether the sessions that loaded the mise skill passing URLs whole (2 of 2) vs the
  no-skill session stripping the gid (1 of 1) is signal or coincidence — N is tiny and
  confounded. Watch: it mildly supports dogape's SKILL.md-first plan.
- `raw=`/`tabs=`/`suggestions=` had zero uses all week — the log cannot distinguish
  "no need" from "feature unknown". Another week of data decides.

### Risks

- **livano is committed but unshipped** — vendored source rides the next
  `/batterie:publish`. Until then installed plugins still mangle `&amp;` links. Batch it
  with dogape's SKILL.md step, which livano was sequenced before.
- The in-session MCP server predates livano; smoke any link-harvesting probe in a fresh
  session (standing gotcha, bites here specifically).
- Google-side numbers in the spike doc are Pre-GA and dated 2026-08-01; the bench handoff's
  accept-and-drop findings (pageSize ignored, phantom pagination, includeComments no-op)
  mean: exercise every parameter observably before relying on it, and re-probe on churn.

### Opportunities

- **mise-dogape** — now unblocked (livano was its prerequisite). Step one is SKILL.md
  ("pass the WHOLE URL"), and the usage review adds its live production instance (the
  stripped gid, 27 Jul 08:57). Natural publish companion for livano.
- **mise-vadoko route-vs-build is decidable NOW** — the preview gate opened 31 Jul.
  Run the index-sheet workflow (the 16-script pattern, prototypes in ~/scratch) through
  sheetsmcp with the accept-and-drop discipline; bazuvo's rich-text half needs REST
  regardless.
- **mise-tuveda / mise-soheka / mise-tezudu / mise-gilojo** — all fully briefed this
  session; tuveda is the direct descendant of the review's headline finding.
- **mise-sijozi** — still wants fresh eyes at a session START, not a tail.
- mise-demasu and mise-wuralu remain deliberate sketches (Mac-side / assembler work) —
  confirmed intentional at this close.

## For Claudes to come

**Where two instruments exist, a verdict from one is a hypothesis.** This session's every
load-bearing single-instrument verdict was wrong or incomplete until the second instrument
ran: the call log said a 404'd document was silently dropped (the transcript showed
recovery seven minutes later by another route); a transcript grep said the mise skill was
never loaded (exact-match showed it loaded twice — the pattern assumed a space CC's compact
JSON doesn't have); a day-old doc reading said the Google preview was gated (the bench
handoff recorded it opening mid-session). The paired corollary that caught two of these
before they shipped: **a number that looks too clean — 0 of 3, 50 of 50 — indicts the
probe's pattern before it describes the world.** Run the known-positive control by default,
not on suspicion. And the production lesson the whole review distilled, worth carrying into
every error message this repo writes: refusals that teach get obeyed in seconds; errors
that don't cost minutes — when a failure path can name the next move, that text is the
cheapest UX surface in the system.
