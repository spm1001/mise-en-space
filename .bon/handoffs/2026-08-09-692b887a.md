# Handoff — 2026-08-09

session_id: 692b887a-53ca-433f-8289-82d7ac4ac6a8
purpose: Morning sweep off the handoff's opportunities — jujoti finished (not narrowed), rafote probed-and-shipped, gosadu's negative recorded; three releases
format: fond-v1

## For the next Claude

### Done
- Synthesized the dogape handoff into understanding.md at open (pointer pattern, manifest-keys-are-presence-contracts, alignment guards) — 9d8e28e. Flagged understanding.md at 11,322 words vs its ~10,000 alarm; adjudicated watch-not-act since real growth since the last pass is ~900 words.
- Swept Sameer's Todoist "& Toolmaking" queue on request: the only mise callout (Sheets range write, mise-vadoko) had already shipped Thursday — completed it there with a resolution note.
- Closed mise-jujoti and shipped suite 1.44.0 (794daf2): search/label provenance rides as `cues.gmail_url_context` (unquote_plus), non-zero `/u/N/` draws a warning cue, and fetch resolves mise's own `#drafts/<r-id>` link via drafts.get → threadId → thread fetch, disclosed with both ids and the do(draft) edit route. Live probe: created a draft, fetched its own emitted URL, got the thread + cue, discarded the probe draft. The 2026-08-08 handoff's "jujoti looks three-quarters shipped, maybe just close it" was wrong in the useful direction — the brief itself recorded the draft-URL clause as unmet, and finishing cost one morning slot.
- Closed mise-rafote and shipped suite 1.46.0 (b9ba9cd): Docs smart chips are WRITABLE — whole-line `@url` in do(create)/do(overwrite) becomes a live-titled richLink. New `tools/doc_chips.py` (create.py is ratchet-frozen; find_placeholder_indices moved there, create tightened 952→931→927). Probes before code bought the contract: uri-only (a supplied title is a 400 and Google enriches server-side — accept-and-enrich, same family as Sheets), non-Workspace URLs refused by name.
- Closed mise-gosadu: Slides chips are a TOTAL negative — zero chip/richLink/person surfaces in the current Request union (45 types) AND the presentations read schema, and a live insertRichLink 400s "Unknown name", so the reference isn't stale. Recorded in understanding.md (3c3bdc5). Dibuje is half done: Docs shipped, Slides ruled out; musepe and zikele remain.
- Close-reflection hardenings shipped as 1.46.1 (ea83911): fenced `@url` lines stay literal (parser now shares convert_fenced_blocks' own fence regex, so the two passes can't disagree about what is code), and a failed image-embed batch restores placeholders to literal `![alt](path)` text via a shared restore_placeholders — no more sentinel residue on either pass.
- Evidence across the morning: 45 new tests (suite 2,258 → 2,308), nine watched-red controls each reddening exactly its predicted set, smoke_stdio 8/8 with a new dead-draft envelope case, three live probes with cleanup, and installed-artefact diffs run at the moment of reliance for all three releases (1.44.0, 1.46.0, 1.46.1 — the gap numbers were bon and trousse lanes publishing concurrently).

### Reflection
Probe-first paid three times in one morning, and the cheapest layer was new: fetch the CURRENT API reference's Request union via devdocs and grep it. That found insertRichLink (absent from training memory), refused the title field before code was written, and settled the Slides negative without building anything. The rafote brief's "do not trust training memory" line was load-bearing both ways.

One self-inflicted incident, fully recovered: watched-red controls that mutate files were "restored" with `git checkout`, which errors on untracked files (the controls silently accumulated — still interpretable only because each added exactly its one predicted failure) and silently reverts EVERYTHING on tracked files (control 4's revert ate create.py's unstaged chip wiring, re-applied from context). Second control round staged everything first and was clean.

The suite version moved under both publishes (bon shipped 1.45.0 and trousse 1.45.1 between my 1.44.0 and 1.46.0) — lanes span hosts, normal traffic, but always read the publish dry-run's version line rather than predicting the number.

### Uncertain
- The chip pass in remote mode is reasoned-fine (API-only, no filesystem) but never live-fired remotely — the whole remote path is parked estate-wide, so this is theory until mise-mawawa revives it.
- Whether a chip written by mise survives a mise fetch→create round-trip as anything better than plain text: extractors/docs.py reads richLink elements, but the deposit renders them to markdown, so a round-trip flattens a chip to a link. Nobody has needed this yet; noting the coupling, not proposing work.

### Risks
- This session's MCP server predates all three releases, and its SKILL.md/CLAUDE.md edits are session-cached — nothing shipped today has been exercised through a live `mcp__` call yet. Deliberate: Sameer offered a reload and we agreed to leave the check to you (see Opportunities).
- DO_DESCRIPTION_FULL sits at 1,934/2,048 with the headroom assert at <100 — the next op addition meets the assert by design; funding came from compressing setup_oauth and copy lines (their detail lives in mise://docs/*).
- DO_DESCRIPTION_REMOTE deliberately doesn't advertise chips; remote create supports them and cues disclose. Adjudicated chill, recorded so it isn't re-found as a gap.

### Opportunities
- **Envelope check first (the dogape reload rhythm): one real fetch of a Gmail search URL through the live plugin (≥1.46.1) should show `cues.gmail_url_context`, and one doc create with a whole-line `@url` should cue `chips_inserted` — that clears today's whole evidence debt in two calls.
- **mise-zikele** — Calendar attachments, last probe-shaped dibuje limb: same recipe as today (devdocs → events Request/resource surfaces → live probe on a throwaway event).
- **mise-musepe** — Gmail include= rendering characterisation: needs eyes on a real rendered message (passe screenshot or Sameer's own client), not just API reads.
- **mise-kacani** — the single-tab round-trip header wart, small, repro steps in the brief.
- **mise-komibu** — comment triage arc, two actions left; the posting-as-Sameer step wants him in the loop.

## For Claudes to come

**The cheapest probe layer for "can API X do Y?" is the Request-union grep, and it settles both directions.** Fetch the current reference for the batchUpdate request union via mit-google-devdocs and grep it: insertRichLink's presence (Docs) and absence (Slides) each cost one call, both contradicted training memory, and the union names EXACTLY what can be attempted — no invented request name survives schema validation. Pair the negative with one live 400 control ("Unknown name … Cannot find field") to prove the doc isn't stale; pair the positive with a write+read-back probe, because field-level contracts (title refused, server-enriched) live below the union's visibility. Docs' chip key is `richLinkProperties.uri` while its plain links are `textStyle.link.url` — the uri/url asymmetry runs INSIDE one product, not just across products.

**Watched-red controls that mutate files: stage everything before the first control, and restore with `git checkout -- <path>` only then.** On untracked files checkout errors and the control mutations silently accumulate across rounds; on tracked files it silently reverts the WHOLE file to HEAD/index, eating unstaged real work along with the control. Both bit in one session (accumulated reds stayed interpretable only because each control added exactly its one predicted failure — diverse narrow predictions are what kept a corrupted harness readable). Staged-first, the same loop ran clean. Edit-tool reverts are the alternative when staging is premature.
