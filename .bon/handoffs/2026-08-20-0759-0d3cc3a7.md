# Handoff — 2026-08-20

session_id: 0d3cc3a7-8e8f-469e-8eab-4058b1c3a013
purpose: ship gujiro (provenance stamps) + walk the calendar roadmap with Sameer, staging the booking run
format: fond-v1

## For the next Claude

### Done
- **Shipped mise-gujiro (suite 1.69.0)**: every insert_event auto-stamps `mise:minted_by`/`minted_at` into extendedProperties.private; `properties={k:v}` on create_event/update_event for programme keys; `list_events(private_property='k=v')` is the reconciler's keyed query. Four live probes before building, all friendly: colon keys accepted, filter matches under singleEvents, recurring instances inherit the master's stamps, and patch MERGES the private map per-key (arrays still replace wholesale).
- **Shipped mise-kawegu (same 1.69.0)**: `color=` on both event ops (name or 1–11, teaching refusal names the palette). Labels are a probed NEGATIVE: eventLabelId accepted a nonexistent id and enriched it to a UUID, and the palette (calendars.get) 403s on our scopes — recorded in events_util.py, docs_do and CLAUDE.md; don't re-surface.
- **Shipped visibility= and transparency= (suite 1.70.0)**, from Sameer's "have we consulted the devdocs?" — a systematic sweep of the events reference found 70 writable properties (mise covered 11). Both probed first; the API omits default values on read-back, `readback_field()` renders that honestly. Remainder of the sweep adjudicated and parked on mise-fodowi.
- **Walked the calendar roadmap with Sameer, order blessed**: booking run → redewa → wavotu+reboto (pogotu's consent click folded in). Filed mise-fodowi (focusTime/OOO auto-decline lane — self-defending GTD blocks) with the parked-fields adjudication AND a six-dialog UI census from his screenshots: five of six create-dialog tabs are API-reachable; Appointment schedule is the one with NO API surface (he runs a live booking page, "ITV Outcome Measurement Meetings"); Task is the separate Tasks API, deliberate non-goal (Todoist is his book).
- Compass: added the wavotu dispatch line to & Toolmaking (tell-after norm); the mitoki and rijeco-build lines are now tickable — both shipped.

### Reflection
The rhythm that worked all session: probe live before building (four probes for gujiro, three for kawegu, three for visibility — every one either confirmed a design or decided a boundary), ship small, publish per batch. Sameer's "have we consulted the devdocs?" was the are-you-sure verification prompt in action — the honest answer was "narrowly", and the systematic sweep it triggered found two shippable fields plus the focusTime lane in under an hour. One self-inflicted CI red, mechanism worth keeping: I edited CLAUDE.md AFTER the local pytest run, and the doc-parity test (which polices prose, not just code) read "the 2026-08-20 sweep" as a claim of 2,026 operations. A policed doc edited after the suite ran is untested code.

### Uncertain
- Whether the wavotu surface shape should be search `calendar_id=` (my stated lean) or freebusy `detail=True` — leaned, not decided; the wavotu session should re-derive against the description budget (104 headroom after this session's params — the next addition likely meets the headroom assert by design).

### Risks
- **The programme is still designed, NOT booked** — every old 1:1 series stands; rijeco's --how holds the strip list (Dan's TVA series and the Tue 13:30 threesome STAY).
- **The booking session must be a fresh session** — this one's MCP envelope still runs 1.68.1; only a restart picks up 1.70.0's properties/color/visibility/transparency.
- GTD blocks and transparency: 'free' shows without eating slot-mining but invites double-booking; 'busy' defends the slot but blocks the allocator's arithmetic for others. Per-block choice, staged through the preview — don't blanket-apply either.
- Room mechanics at booking time unchanged from the last handoff: re-check room freebusy at booking, read each room's responseStatus in the create read-back (rooms auto-decline silently).
- mise-ruvibu (credential-bearing email deposits secrets in cleartext) arrived from another lane mid-session — real and untriaged; don't let the calendar momentum bury it.

### Opportunities
- **The booking run is fully armed (mise-rijeco step 5)**: fresh session, draw down rijeco, mine 15/22 Sep, allocator over the banked spec — events can now be stamped (`properties`), coloured, and metadata'd from birth; one approving pass through preview gates.
- **mise-redewa right after**: intent file from rijeco's spec + first hand-run reconciliation — the stamps make it one keyed query (`list_events(private_property='mise:programme=…')`).
- **mise-wavotu + mise-reboto batch as one session** (queue line added); fold mise-pogotu's scope-consent click into it.
- **mise-fodowi** carries the focusTime probe plan AND the parked writable-fields adjudication — read its --how before any calendar-metadata work, the sweep is recorded there.

## For Claudes to come

**A test suite that polices prose makes doc edits code — run it AFTER the last edit, whatever the edit touched.** This session's only CI red came from rewording CLAUDE.md after the local pytest run: test_doc_operation_parity greps the markdown for numbers near the word "ops", and "event ops too, from the 2026-08-20 sweep" parsed as a claim of 2,026 operations. The suite was green locally because the doc edit hadn't happened yet — the same unearned-green shape as testing a cheaper layer, wearing documentation's clothes. In this repo the policed prose surfaces are CLAUDE.md, README.md, SKILL.md and resources/docs.py (parity + budget + guidance-freshness tests); the discipline is that the full suite runs after the LAST write of any kind, and a "docs-only" tweak re-arms the requirement.

**Google Calendar's write surface, now fully mapped (2026-08-20): 70 writable properties, and the API omits fields at their default value on read-back.** The omission fact generalises — a read-back check that treats absence as "write dropped" false-alarms on every default-valued write; `readback_field()` in events_util is the pattern (absence after requesting the default is success). And the patch asymmetry is now probed both ways: arrays replace wholesale, the extendedProperties.private map merges per-key. The UI census (six create-dialog tabs) mapped five to the API and one to nothing — appointment schedules have no API surface at all — with the whole adjudication recorded on mise-fodowi's brief rather than in prose that would rot.
