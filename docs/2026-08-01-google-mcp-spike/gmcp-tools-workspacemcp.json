[
 {
  "annotations": {
   "destructiveHint": false,
   "idempotentHint": true,
   "openWorldHint": false,
   "readOnlyHint": true,
   "title": "Search Workspace Corpora"
  },
  "description": "Performs a search across multiple Workspace corpora (Drive, Gmail, Calendar, and Chat) based on the provided query.\n\nThis tool is a cross-corpus search tool that by default fans out to all supported corpora (Drive, Gmail, Calendar, and Chat). If the user's query does not specify a particular corpus (e.g., \"where am i planning to go for my photography walks in june?\"), you MUST call this cross-corpus \"search_corpus\" tool first, rather than calling specific tools like \"list_events\", \"search_threads\", \"list_threads\", or \"search_files\". If this tool returns no results, you can then fallback to these per-product (single-corpus) search tools.\n\nNote that if the user specifies a particular corpus in their query (e.g., \"search for content in my email\"), it is recommended to use a single-corpus tool, such as the GmailTool, for more focused results if available. However, this cross-corpus search tool will still function for those queries, and should be used if specific tools are not available.\n\nThis tool can filter results based on a query string. Pagination is not supported for cross-corpus search. Note that if a Gmail message contains attachments, they are not returned as a separate field; instead, attachment details will be included directly within the message's snippet.\n",
  "inputSchema": {
   "description": "Request message for WorkspaceToolService.SearchCorpus.",
   "properties": {
    "pageSize": {
     "description": "Optional. The maximum number of results to return. If not set, a default page size will be used.",
     "format": "int32",
     "type": "integer"
    },
    "pageToken": {
     "description": "Optional. The page token to use for pagination.",
     "type": "string"
    },
    "query": {
     "description": "Required. The raw query string provided by the user.",
     "type": "string"
    }
   },
   "required": [
    "query"
   ],
   "type": "object"
  },
  "name": "search_corpus",
  "outputSchema": {
   "$defs": {
    "CalendarResult": {
     "description": "Represents a search result from Google Calendar.",
     "properties": {
      "calendarId": {
       "description": "The calendar id of the Calendar event, which is the owner's email address.",
       "type": "string"
      },
      "description": {
       "description": "The description of the Calendar event.",
       "type": "string"
      },
      "eventId": {
       "description": "Unique identifier for the Calendar event.",
       "type": "string"
      },
      "title": {
       "description": "The title of the Calendar event.",
       "type": "string"
      },
      "viewUrl": {
       "description": "The URL to view the Calendar event.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "ChatAttachment": {
     "description": "Metadata for an attachment in a chat message.",
     "properties": {
      "attachmentId": {
       "description": "Resource name of the attachment.",
       "type": "string"
      },
      "filename": {
       "description": "Name of the attachment.",
       "type": "string"
      },
      "mimeType": {
       "description": "Content type (MIME type).",
       "type": "string"
      },
      "source": {
       "description": "The source of the attachment.",
       "enum": [
        "SOURCE_UNSPECIFIED",
        "DRIVE_FILE",
        "UPLOADED_CONTENT"
       ],
       "type": "string",
       "x-google-enum-descriptions": [
        "Reserved.",
        "The file is a Google Drive file.",
        "The file is uploaded to Chat."
       ]
      }
     },
     "type": "object"
    },
    "ChatMessage": {
     "description": "Represents a message in a chat conversation.",
     "properties": {
      "attachments": {
       "description": "Attachments included in the message.",
       "items": {
        "$ref": "#/$defs/ChatAttachment"
       },
       "type": "array"
      },
      "createTime": {
       "description": "Timestamp when the message was created.",
       "format": "date-time",
       "type": "string"
      },
      "id": {
       "$ref": "#/$defs/ChatMessageId",
       "description": "Unique identifier for the message and thread."
      },
      "plaintextBody": {
       "description": "Plain text body of the message.",
       "type": "string"
      },
      "reactionSummaries": {
       "description": "The emoji reactions summary included in the message.",
       "items": {
        "$ref": "#/$defs/ChatReaction"
       },
       "type": "array"
      },
      "sender": {
       "$ref": "#/$defs/ChatUser",
       "description": "The sender of the message."
      },
      "threadedReply": {
       "description": "Whether message is a thread reply.",
       "type": "boolean"
      },
      "viewUrl": {
       "description": "The URL to view the Chat message.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "ChatMessageId": {
     "description": "Represents a unique identifier for a chat message.",
     "properties": {
      "messageId": {
       "description": "Resource name of the message.",
       "type": "string"
      },
      "threadId": {
       "description": "The thread this message belongs to.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "ChatReaction": {
     "description": "Summary of emoji reactions on a chat message.",
     "properties": {
      "count": {
       "description": "The total number of reactions using the associated emoji.",
       "format": "int32",
       "type": "integer"
      },
      "emoji": {
       "description": "The emoji unicode string or custom emoji name.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "ChatResult": {
     "description": "Represents a search result from Google Chat.",
     "properties": {
      "messages": {
       "description": "A list of messages that could be grouped together as a conversation.",
       "items": {
        "$ref": "#/$defs/ChatMessage"
       },
       "type": "array"
      }
     },
     "type": "object"
    },
    "ChatUser": {
     "description": "Represents a user in Google Chat.",
     "properties": {
      "chatUserType": {
       "description": "The type of the user.",
       "enum": [
        "CHAT_USER_TYPE_UNSPECIFIED",
        "CHAT_USER_TYPE_HUMAN",
        "CHAT_USER_TYPE_BOT"
       ],
       "type": "string",
       "x-google-enum-descriptions": [
        "Default value. Do not use.",
        "Human user.",
        "Bot user."
       ]
      },
      "displayName": {
       "description": "The display name of the user.",
       "type": "string"
      },
      "email": {
       "description": "The email address of the user.",
       "type": "string"
      },
      "userId": {
       "description": "Unique identifier for the user.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "DriveResult": {
     "description": "Represents a search result from Google Drive.",
     "properties": {
      "contentSnippet": {
       "description": "The content snippet of the Drive item.",
       "type": "string"
      },
      "itemId": {
       "description": "Unique identifier for the Drive item (e.g., document, sheet, presentation).",
       "type": "string"
      },
      "mimeType": {
       "description": "The mime type of the Drive item.",
       "type": "string"
      },
      "title": {
       "description": "The title of the Drive item.",
       "type": "string"
      },
      "viewUrl": {
       "description": "The URL to view the Drive item.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "GmailMessage": {
     "description": "Represents a message from Gmail.",
     "properties": {
      "ccRecipients": {
       "description": "The cc recipients' email addresses of the Gmail message.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "messageId": {
       "description": "Identifier for the Gmail message.",
       "type": "string"
      },
      "recipients": {
       "description": "The recipients' email addresses of the Gmail message.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "sender": {
       "description": "The sender's email address of the Gmail message.",
       "type": "string"
      },
      "snippet": {
       "description": "The content snippet of the Gmail message.",
       "type": "string"
      },
      "subject": {
       "description": "The subject of the Gmail message.",
       "type": "string"
      },
      "viewUrl": {
       "description": "The URL to view the Gmail message.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "GmailResult": {
     "description": "Represents a search result from Gmail.",
     "properties": {
      "thread": {
       "$ref": "#/$defs/GmailThread",
       "description": "The thread that match the search query."
      }
     },
     "type": "object"
    },
    "GmailThread": {
     "description": "Represents a thread from Gmail.",
     "properties": {
      "messages": {
       "description": "A list of messages in the thread.",
       "items": {
        "$ref": "#/$defs/GmailMessage"
       },
       "type": "array"
      },
      "threadId": {
       "description": "Identifier for the Gmail thread.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "SearchResultItem": {
     "description": "Represents a single search result item, which can originate from various corpora.",
     "properties": {
      "calendarResult": {
       "$ref": "#/$defs/CalendarResult",
       "description": "A search result from Google Calendar."
      },
      "chatResult": {
       "$ref": "#/$defs/ChatResult",
       "description": "A search result from Google Chat."
      },
      "driveResult": {
       "$ref": "#/$defs/DriveResult",
       "description": "A search result from Google Drive."
      },
      "gmailResult": {
       "$ref": "#/$defs/GmailResult",
       "description": "A search result from Gmail."
      }
     },
     "type": "object"
    }
   },
   "description": "Response message for WorkspaceToolService.SearchCorpus.",
   "properties": {
    "items": {
     "description": "A list of universal search result items, each potentially from a different corpus.",
     "items": {
      "$ref": "#/$defs/SearchResultItem"
     },
     "type": "array"
    },
    "nextPageToken": {
     "description": "The next page token to use for pagination.",
     "type": "string"
    }
   },
   "type": "object"
  }
 }
]