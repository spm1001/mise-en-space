# Toise: mise-en-space — 2026-06-10

Structural health check (Cracks/Dustballs/Goofs, three parallel Opus agents) run as
mise-canumo, with the 2026-06-09 cold-review findings passed in as priors to confirm
or refute. Metrics: 175 source files, p50 190 lines, p90 639, max 2878; first-breath
100%; CLAUDE.md 3/5 sections.

## The honest answer

You're fine — and better than fine where it counts. The layered code (adapters,
extractors) is the strongest Claude-maintained shape toise has graded: 100%
first-breath, one predictable pattern across every sibling, mechanically enforced
purity, zero dead code, zero TODOs. The one structural weakness is an **enforcement
asymmetry**: `test_architecture.py` polices only `extractors/` and `adapters/`, and
the codebase's mass has accumulated precisely in the tier it cannot see — `server.py`
(1,318 lines) and `tools/fetch/gmail.py` (903 lines). That isn't decay; it's water
finding the unpoliced gap. The fix is already on the board (mise-jimohe + the new
mise-wugehi), and the keystone move is extending `LAYER_RULES` jurisdiction, which
makes the discipline self-maintaining instead of vigilance-dependent. Nothing here is
urgent; all of it is tractable.

## Grades

| Principle | Grade | Verdict | Agent |
|-----------|-------|---------|-------|
| 1. Self-documenting | A− | Adapters/extractors exemplary; generic-name flags are false positives with real docstrings | Cracks |
| 2. One shape (pattern quality) | A | Could predict any adapter/extractor from a sibling; textbook | Cracks |
| 2. One shape (adherence) | B− | Error-handling contract honoured by drive/gmail, ignored by the conversion family | Dustballs |
| 3. Boundaries | B− | 3-layer boundary documented AND mechanically enforced — but server.py, workspace/, root utils unpoliced | Cracks |
| 4. Small/pure/explicit | B− | Extractor purity intact; gmail.py tangled (903 lines, 10 name-reuse mypy errors); cache authority scattered across 3 files | Dustballs |
| 5. Extend by recipe | A− | Recipes current; Forms followed them faithfully; drift is in the maps (adapter table), not the recipes | Goofs |

## Prior verdicts (2026-06-09 review, reconciled per mise-canumo)

| Prior | Verdict | Evidence |
|-------|---------|----------|
| jimohe (server.py ≠ "just registers tools") | **CONFIRM** | 1,318 lines: _DISPATCH ~105, _REQUIRED_PARAMS ~74, remote orchestrators, ~530 lines of resource docstrings |
| zedoli (deposit-wipe landmine) | **CONFIRM structurally, REFUTE as live bug** | All ~20 call sites call once per fetch path; no retry loop contains one. Preventive hardening, not active vulnerability |
| dibudi (discovery-by-error unpriced) | **SHARPEN** | All 14 _REQUIRED_PARAMS entries verified correct vs do_* implementations. Validation sound; only the UX enrichment remains |
| bigiko (no deposit disposal) | **CONFIRM** | No cleanup/TTL/prune anywhere in workspace/. Nuance: this repo's .gitignore covers /mise/; cross-repo accumulation is the unsolved half |
| lijogi (docs drift) | **CONFIRM + extended** | README verb table says create (real: do/13 ops); "Beta" status stale; CLAUDE.md adapter table omits calendar/cdp/charts/forms; extractor-rule wording diverges README vs CLAUDE.md |
| gmail-mass (2026-06-10 session) | **CONFIRM** | 903 lines (sharpened from ~950); 10 mypy errors all name-reuse; triple PDF/image dispatch is gmail.py-local, not repo-wide |
| cross-cutting gaps (mugure→dazode shape) | **REFUTE — broadly clean** | validate_* uniform across all 10 do_* tools; supportsAllDrives on every Drive call; with_retry consistent. The dazode instance was the only one, and it's fixed |

## Cracks (structural)

1. **Enforcement asymmetry** — LAYER_RULES in test_architecture.py cover only
   extractors/adapters; server.py, workspace/, and six root utilities import-free.
   Mass accumulates exactly there. Fix via mise-jimohe step 3 (extend jurisdiction).
2. **Deposit-wipe landmine guarded by prose alone** (manager.py:103-105) — mise-zedoli,
   now correctly priced as preventive.

## Dustballs (drift & cruft)

1. **Error-contract drift** — conversion family (office, pdf, conversion, forms, image,
   activity, calendar) never converts to MiseError despite CLAUDE.md's claim. Filed as
   **mise-ceroru** (conform or document the two-tier reality).
2. **gmail.py tangle** — filed as **mise-wugehi** (split along three concerns, unify
   dispatch, normalise return shapes).
3. **Cache authority scattered** — http_client.py manual singletons, filters.py two
   lru_caches (undocumented in CLAUDE.md's table), drive.py:579 manual cache. Folded
   into mise-lijogi's sweep.
4. **Genuinely clean otherwise** — no dead code, no TODOs, no commented-out blocks
   anywhere in adapters/tools/extractors (conf 0.90).

## Goofs (bugs & staleness) — all fixed in-session

1. ~~ContentType Literal missing "form"~~ (workspace/manager.py:19 vs
   tools/fetch/drive.py:423) — **fixed** in the toise quick-wins commit.
2. ~~Path containment via str.startswith~~ (tools/create.py:307, prefix-collision
   admits /repo-evil siblings) — **fixed**, now Path.is_relative_to.
3. ~~understanding.md staleness~~ (services.py deleted but described as alive; morning's
   own current-state notes outdated by the afternoon's fixes) — **fixed**.
4. Leaked log file handle in tools/setup_oauth.py:118 (parent never closes; passed to
   detached child) — note-severity; lands with mise-jiberu's setup_oauth test work.

Non-findings verified clean: query-string escaping present and used; slug
path-traversal impossible ([a-z0-9-] only); do() descriptions 921/1107 chars vs 2048
ceiling; subprocess uses list args. Only tools/plain_file.py, tools/setup_oauth.py,
resources/tools.py lack dedicated test files.

## Proposed CLAUDE.md additions (pending maintainer accept/edit/reject)

1. **Layer-rules addendum:** "Shared-root utilities and server.py are NOT covered by
   test_architecture.py. Its LAYER_RULES police only extractors/ and adapters/. Root
   utils (cues_util.py, filters.py, etc.) and server.py may import freely — keep them
   dependency-disciplined by hand, and never let a root util import tools/. If this
   tier grows, extend LAYER_RULES to cover it." *(Interim text — superseded when
   jimohe actually extends the rules.)*
2. **Architecture-table correction:** server.py described as "FastMCP entry point:
   tool/resource registration plus the _DISPATCH table, _REQUIRED_PARAMS validation,
   remote orchestrators, and do() descriptions (~1,300 lines). Largest non-test file —
   treat as a layer, not glue." *(Lands with jimohe step 4.)*
3. **Gotchas entry:** "Deposits never self-dispose. mise/ folders accumulate per-cwd
   forever; no TTL/LRU exists (tracked: mise-bigiko). When a fetch path grows, do not
   assume cleanup happens downstream." *(Lands with bigiko step 3.)*
