# Handoff — 2026-04-08

session_id: a2f2d66c-be4f-4810-b736-25bf8dd803fc
purpose: Research Gmail MCP best practices, file mise-wiboka outcome, deliver quick wins (vosura, dehomu)
format: fond-v1

## For the next Claude

### Done
- **mise-wiboka** (filed) — "Mise allows flexible Gmail handling and manipulation without send or delete." New outcome with 6 actions, born from ecosystem research comparing 3 other Workspace MCPs.
- **mise-vosura** (closed) — Gmail search operator reference exposed as `mise://docs/gmail-search` MCP resource. Comprehensive tested operator table (is:, in:, from:, label:, category:, newer_than:, AROUND, boolean). Overview resource updated to list it. ToolSearch verification deferred to next session (MCP restart needed).
- **mise-dehomu** (closed) — `label_ids` field added to `EmailMessage` model, populated from `msg["labelIds"]` (was already in API response, just discarded). `GmailSearchResult` now has `is_unread` and `label_ids`. Search JSON output includes `is_unread` and `labels`. Fetch cues include `unread_messages` count and `notable_labels` (STARRED, IMPORTANT). 6 new unit tests, 1557 total passing.
- **mise-putadu** converted from standalone outcome to action under mise-wiboka.
- Understanding.md rewritten with MCP property drop knowledge from previous handoff.

### Reflection
- The research phase (3 parallel agents: current capabilities, workspace skill, ecosystem) was the session's real value. It produced findings that made the bons genuinely useful rather than speculative.
- Two key discoveries: (1) the workspace skill has zero coverage of Gmail write operations that shipped Feb 2026 — a Claude loading the skill doesn't know draft/reply_draft/archive/star/label exist; (2) we were the only Workspace MCP not capturing labelIds, which blocks the fundamental "is this thread unread?" question.
- Sameer caught me being glib about --how section quality. Re-reading the bons honestly revealed gaps in mise-zolile (list_labels is architecturally different from the other ops) and mise-jokuwo (undeclared dependency on zolile). Both enriched.

### Risks
- Three repos cloned to `~/Repos/third-party/` (google_workspace_mcp, Gmail-MCP-Server, google-workspace-mcp) — useful reference for batch operation patterns in mise-jokuwo but disk clutter if forgotten.
- The `mise://docs/gmail-search` resource hasn't been verified via ToolSearch — needs MCP restart. Should work (module imports clean, 1557 tests pass) but verify early next session.

### Opportunities
- **mise-zolile** (inbox management ops) is the natural next action — mark_read, mark_unread, unstar, list_labels. The how section has specific guidance including the list_labels architectural difference and remote mode considerations.
- **mise-jokuwo** (batch ops) depends on zolile but is the payoff — it's what turns "Claude can manipulate email" into "Claude can triage an inbox."
- **mise-putadu** (pagination) is independent and well-briefed with specific code path (`adapters/gmail.py:271-274`).
- **mise-dobida** (skill update) should come last, after the features exist.

## For Claudes to come

### Gmail ecosystem patterns

Three Workspace MCP servers were evaluated (cloned to `~/Repos/third-party/`): taylorwilsdon/google_workspace_mcp (2k stars), GongRzhe/Gmail-MCP-Server (1k stars), aaronsb/google-workspace-mcp (143 stars).

**Safety model spectrum:** GongRzhe has no safety — permanent batch deletion without confirmation. taylorwilsdon has a hardened fork that removes dangerous code entirely. aaronsb has the gold standard: a composable policy engine (`src/factory/safety.ts`) with `allow`/`block`/`downgrade` actions — "downgrade" silently converts send to draft, reducing blast radius without blocking the agent. Mise's remote mode operation gating (`_REMOTE_ALLOWED_OPS`) is spiritually similar but coarser (binary allow/block, no downgrade). If mise ever needs finer-grained safety, aaronsb's pattern is the reference implementation.

**Batch operations are table stakes for triage.** taylorwilsdon has `batch_modify_gmail_message_labels`. Without batch, triaging an inbox of 200 emails means 200 sequential tool calls. Mise's existing `do(move)` already accepts `file_id` as a list for Drive — extending `archive`/`label`/`mark_read` to accept lists is the natural pattern. The Gmail API also has a `batchModify` endpoint (single call for multiple threads) worth investigating for performance.

**labelIds is universally captured.** Every other server stores Gmail label IDs from the API response. Mise was the outlier — we requested `labelIds` in `THREAD_FIELDS` and `SEARCH_THREAD_FIELDS` but discarded them during message parsing. Now fixed (mise-dehomu). This field is the foundation for "show me unread threads" → fetch → "archive the read ones" workflows.

**Content extraction is where mise leads.** Signature stripping (talon), eager attachment extraction, markitdown HTML conversion, Drive pre-exfil optimisation — mise's fetch quality is ahead of all three competitors. The gap is in breadth of Gmail actions and discoverability of search syntax, not in content processing.
