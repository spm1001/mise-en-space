# Handoff — 2026-06-10

session_id: ff650a1e-a1a6-4d62-91a6-edb4a5be0ccd
purpose: Gmail trust follow-ups shipped as 0.7.4, toise structural sweep (canumo), and the jimohe server.py shrink with enforcement extension
format: fond-v1

## For the next Claude

### Done
- /open synthesis caught a 3-week gap: the May 18 handoff's "For Claudes to come" (trust-briefs-over-synthetic-tests) had never been integrated — now a section in understanding.md, alongside the estate-audit residue (board consciously triaged 2026-06-09/10; don't re-triage)
- Cleared all 4 Dependabot alerts: urllib3 2.7.0 (high ×2), starlette 1.2.1 (across the 1.0 major — remote boot smoke-tested), idna 3.18
- Closed **mise-nucupi** (e0ba8b6): participants dedup on canonical email via getaddresses, best display form wins. Verified live: BARB thread 19df8005ab2c611e returns 5 unique (was 6), held across 15 messages
- Closed **mise-dazode** (c116edb): eager-extraction loop resolves Outlook octet-stream MIME like fetch_attachment does. Verified live: a real Outlook-tagged requisition PDF eagerly extracted with cue warning
- Released **0.7.4** (535f010) and propagated it: bot-dispatched batterie assemble.yml (clean clones — the repo's own guard says local assemble vendors working-tree cruft), marketplace + hezza plugin cache now at 0.7.4. Closed **mise-nowawi**
- Closed **mise-canumo**: /toise with the 2026-06-09 review findings as priors — three Opus agents, full report at docs/toise-2026-06-10.md. Verdicts: jimohe/bigiko/lijogi/gmail-mass CONFIRMED, zedoli REFUTED-as-live-bug (all ~20 call sites single-call), dibudi premise SHARPENED (_REQUIRED_PARAMS verified correct). Grades P1 A−, P2 A/B−, P3 B−, P4 B−, P5 A−. Filed **mise-wugehi** (gmail.py split) and **mise-ceroru** (adapter error contract); sharpened all five sibling briefs with verdicts
- Closed **mise-jimohe** (3e1f6ef): server.py 1,318 → 344 lines. Resource text → resources/docs.py; remote orchestration → tools/remote.py; dispatch machinery + run_operation() → tools/dispatch.py. _REMOTE_MODE stays at module load (decoration-time constraint respected). test_architecture.py jurisdiction extended: LAYER_RULES += workspace/ + resources/; FILE_RULES polices the root tier; server.py capped at 500 lines
- Closed **mise-lijogi** (cb6922b): README verb table says do/14 ops (was create), status Stable (was Beta), auth flags corrected (--manual never existed; it's --auto/--code), broken skill-section code fence fixed, claude-suite → chrome-debug; CLAUDE.md adapter table gained calendar/forms/charts/cdp, shared-utilities table gained cues_util + a caches-enumerated note
- Toise quick wins fixed in-session: ContentType Literal missing "form"; str.startswith path containment → is_relative_to; setup_oauth parent log-fh leak; FILE_RULES converted from enumeration to discovery (new root files get the strict default automatically); CHANGELOG gained an Unreleased section covering the refactor

### Reflection
- The toise-with-priors pattern worked exactly as the brief hoped: reconciliation produced two REFUTATIONS that down-priced work (zedoli is preventive not urgent; dibudi's table is sound) — as valuable as the confirmations
- Real-layer smoke tests earned their keep twice: the starlette 1.x bump (remote boot + /health) and the jimohe refactor, where booting --remote caught a stale `_REMOTE_ALLOWED_OPS` reference in the `__main__` block that no unit test can reach
- pytest's "N passed" summary line vanishes when piped in this environment — never diagnosed; exit codes are the reliable signal. Don't burn calls chasing the count
- My own morning understanding.md update was stale by afternoon (toise flagged claims about work I'd done in between). Current-state prose rots fastest

### Risks
- **This session's mise plugin still runs 0.7.3-era code** — the 0.7.4 cache activates on next session restart. If a fresh session sees old behaviour, check `installed_plugins.json` version first
- **Mac plugin cache is still 0.7.3** — run /batterie:update in the next Mac session
- Remote mode on starlette 1.2.1 was verified boot + /health + conditional description only, not a full MCP round-trip over HTTP. It's a parked path; whoever revives it should E2E it
- The dazode resolution warning fires for resolved-but-not-eagerly-extracted attachments too (a CSV in a thread fetch gets "treating as text/csv" with no inline extraction — forward-pointing, fetch_attachment will honour it). Watch for warning fatigue alongside zojoma's >80% strip warning; don't tune either without usage data
- Test patch targets moved with the refactor: remote paths patch `tools.remote.do_fetch`/`do_search`, metadata prefetch patches `tools.dispatch.get_file_metadata`, create patches `tools.dispatch.do_create`. Any stale branch touching those tests will conflict

### Opportunities
- **mise-wugehi** (gmail.py split) is now unblocked — jimohe landed the enforcement, so the new modules arrive under policed jurisdiction. Its brief carries the split plan and the mypy-errors-as-progress-signal trick
- **mise-ceroru** (adapter error contract) and **mise-bigiko** (deposit gc) each open with a design decision — good fresh-head openers
- **mise-dibudi** wants a calls.jsonl mining pass before any code; **mise-zedoli** is correctly down-priced preventive hardening
- novanu's done criterion needs the two deposit-wipe gotcha entries replaced by mechanism — zedoli is the remaining gate on that

## For Claudes to come

Mass accumulates exactly where mechanical enforcement can't see. Mise's layered code stayed small and pure for a year because test_architecture.py policed it; server.py and tools/fetch/gmail.py — outside its jurisdiction — quietly grew to 1,318 and 903 lines while CLAUDE.md claimed "server.py just registers tools." An oversized module in a well-policed codebase signals a jurisdiction gap, not a discipline failure: the fix is extending the rules, not just cleaning the module, because the rules make the cleanup self-maintaining — and prefer discovery to enumeration when writing such rules (a glob picks up new files automatically; a hardcoded list is itself a future jurisdiction gap). Two companions from the same session: when a structural review runs with prior findings as priors, the refutations are as valuable as the confirmations — toise down-priced two of five planned actions, which is triage gold, not wasted effort; and understanding.md current-state claims rot within hours when the session keeps working — the morning's own synthesis described as open three things the afternoon fixed. Date current-state claims, or point at the board instead of restating it.
