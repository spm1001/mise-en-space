# Handoff — 2026-07-15

session_id: 31d98034-71fe-4d32-af6b-7b72a87d7e31
purpose: cebopo "warmup" became a full itv-appscript auth + live diagnosis — ongoing trigger re-armed; a never-deployed code drift + a Cloud-Logging gap discovered
format: fond-v1

## For the next Claude

### Done
- **/open synthesis** — integrated the previous handoff's durable knowledge into `understanding.md`: the Dependabot→ratchet trigger (composed onto the shared-bus note), rejula's catastrophic-strip revert + "the call log is blind to content-quality failures," the sogelo gate-keys-on-its-own-state pattern, and the ship log carried to 1.9.3.
- **Authed `itv-appscript`** for the Email Attachment Extractor — full headless jeton/PKCE consent dance (URL minted → Sameer consented → `--code` exchange). `apps-script/token.json` written (8 scopes + refresh token, **gitignored**).
- **Created `apps-script/deploy.json`** (gitignored). The credentials fork was resolved *empirically*: a read-only API-enablement check showed mise's own client (`../credentials.json` = `mit-workspace-mcp-server`) has script+logging+drive+gmail all enabled, while the slides-formatter client lacks Gmail — so mise's own client is correct (reversing my initial "use the tool's default" lean and the doctor's generic hint).
- **Root-caused the stall** — verified the brief's diagnosis against `Code.gs`, then pulled live state: the `processNewEmails` (CLOCK) trigger's `lastRun` cursor is frozen at **2026-03-09**, lifetime `processedCount` 101,158, and a dry run (`testBackfillDryRun`) is **healthy today** (48 processed / 12 uploads / 4 shortcuts / 0 errors). No Google disable-email (Sameer checked Gmail), so the historical *why* is unknowable — and moot, because the machinery works now.
- **Re-armed the ongoing trigger** — `run setupOngoingTrigger` (removed 1 husk, created a fresh 15-min trigger). This restores ongoing new-mail capture on the proven *old* server code. **The core revival is done.**
- **Discovered the deploy-drift** (below) and rewrote the cebopo brief with full current state + a revised 5-step sequence.

### Reflection
- Following the "warmup" energy into a real session was the right call — the auth dance + live state was the whole unlock, and it turned "is cebopo heavy?" into a precise map.
- I misstated the cursor behaviour early ("it auto-catches-up at 50/run"). Sameer's "does the backfill just happen into the bargain?" made me trace it and correct: it does **not** — `backfillSince` does one un-paged search then `updateStats` sets `lastRun=now`, so after one fire the cursor jumps to today and strands the gap. Verify the mechanism before asserting it.
- Observability-first (Sameer's steer) is the right opener for next session — every dead-end tonight (why it died, why `chunkBackfillAll` errored) was an observability gap, not a code gap. Our own kube-cameras lesson, re-fired.

### Uncertain
- **Why the trigger died on 2026-03-09** — no disable email, no logs; unknowable. Moot for the fix but means recurrence can't be ruled out.
- **Whether the re-armed trigger will actually self-fire** — unverified (needs ~15 min; do it as the durability check).
- **Whether Cloud Logging's emptiness is purely the GCP-link** or something more — the incomplete editor link is the likely cause but unconfirmed.

### Risks
- The re-armed trigger runs the **old** server `processNewEmails`; the planned deploy will change what the live trigger executes. Deploy the refactored code **with eyes** — pull server HEAD, diff against local src (they diverged in structure *and* function inventory), and confirm the new `processNewEmails` works before trusting it.
- `deploy.json` + `token.json` are gitignored and local to **tube**. A session on another machine must recreate `deploy.json` (values are in the cebopo brief `--how`) and re-auth.
- Deploying never-deployed refactored code over a live production trigger is the meaty step — do it fresh, with Cloud Logging wired first, not tired.

### Opportunities
- **mise-cebopo** — revised sequence, start at ①: **① wire Cloud Logging** (browser: editor Project Settings → link GCP project, paste project number `413373784317` → `mit-dev-362409`) → **② deploy the refactored src** (diff server vs local first) → **③ watched backfill** of the 2026-03→now gap (`chunkBackfillAll`, dedups) → **④ add ongoing Doc-link capture** (`processNewDriveLinks`, the ~25-line mirror). Then re-vendor + suite bump.
- **Verify the re-armed trigger self-fired** — `processes list` shows a CLOCK run and `lastRun` advanced past 2026-03-09.
- Board unchanged otherwise: mise-nigogi, mise-wemuki, mise-jebude, mise-lirugi (none urgent).

## For Claudes to come

**The `apps-script/` subsystem — operational knowledge (understanding.md currently has none).**

- **Local `src/` is a NEVER-DEPLOYED refactor; server HEAD ≠ local.** The running script is an older *monolithic* version (2182 lines vs local 1961; config inline in `Code.gs`, **no separate `Config.gs`, no `BACKFILL_YEARS`**; the function inventory has diverged). **`itv-appscript run --dev` executes SERVER HEAD, not the code you read locally** — so reasoning about the live script from local `Code.gs` will mislead you. Before any backfill/enhancement, `pull` server HEAD to a temp dir and diff. `chunkBackfillAll` fast-fails (1.1s = immediate missing-reference on the stale code, *not* a timeout); the fix is to deploy the refactored local code.
- **Driving `itv-appscript` against it:** `deploy.json` (gitignored) — scriptId `1m8DCw3H_HMy6DFwJCp2u2h4O06GuTTMwmDT_EDqDIdtQIBZtKqcXhqg8`, gcpProjectId `mit-dev-362409`, credentials `../credentials.json`, filterConfig `../config/attachment_filters.json`. **Use mise's OWN client** (`../credentials.json` = `mit-workspace-mcp-server`) — it has script+logging+drive+gmail APIs all enabled; the slides-formatter client the doctor hints at lacks Gmail. Auth is a headless jeton dance: `itv-appscript auth` prints the URL and saves the PKCE verifier to disk; exchange the redirect URL with `itv-appscript auth --code '<url>'`. (`env -u DISPLAY` forces the headless path in a CC Bash shell.)
- **Observability channels split:** `itv-appscript processes list` **works** (STATUS/FUNCTION/DURATION — your primary window). `itv-appscript logs` (Cloud Logging) is **dark** — the Apps Script editor GCP-project link to `mit-dev-362409` is likely incomplete (a browser step: paste project number `413373784317` in editor Project Settings). Wire it for stack-trace visibility before deep debugging.
- **`processNewEmails` does NOT self-backfill.** `backfillSince` does a single un-paged `GmailApp.search(query, 0, 50)`, then `updateStats` sets `lastRun = new Date()` (Code.gs:280,791) — so one fire grabs 50 and the cursor jumps to *now*, stranding any larger gap. Backfill is a separate lever (`chunkBackfillAll` / `setupTriggers`), which pages per-year with offset checkpoints and dedups against Drive (Message ID + Content Hash) — idempotent, safe to re-run.
- **The extractor's real functional gap (WHAT-6):** there is no *ongoing* Doc-link capture. `processNewEmails` is `has:attachment` only; the Doc-link path (`backfillDriveLinks`/`chunkDriveLinksAll`) is backfill-shaped with no incremental cursor. Pure no-attachment Gemini-note emails are missed ongoing. The fix is a ~25-line `processNewDriveLinks` mirroring `processNewEmails` with its OWN `lastRunDriveLinks` cursor key (verified clean: `backfillDriveLinks` does not call `updateStats`, so no cursor clash) + a `setupOngoingDriveLinksTrigger`.
