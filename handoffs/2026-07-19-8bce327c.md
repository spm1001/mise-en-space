# Handoff — 2026-07-19

session_id: 8bce327c-9c17-4503-86a2-63301113e815
purpose: Diagnose + fix mise auth (revoked itv token), root-cause the revocation to off-box, capture re-auth robustness fixes
format: fond-v1

## For the next Claude

### Done
- **Diagnosed mise auth as broken** — the `sameer.modha@itv.com` refresh token was **revoked** (`invalid_grant: Token has been expired or revoked`), confirmed by a live refresh test, not inferred. Timeline: worked at 09:38, dead by ~11:10 (2026-07-19).
- **Fixed it** via the account-agnostic `--code` path (`uv run python -m auth` print → Sameer clicked in an itv-signed browser → pasted redirect → `--code` redeem). **Verified end-to-end**: force-refresh OK + live Drive `about.get` as `sameer.modha@itv.com`. Cleared the spent `.pkce_state.json`.
- **Confirmed ADC/Vertex safe** — the openwrt session's planetmodha `application-default login` did NOT clobber the default ADC (still `mit-cornichon`/ITV, `chmod 444` guard held, mints a token fine).
- **Root-caused the revocation across ALL tube sessions** (deja + deglacer): **no local trigger** — no mise-work-client (`413373784317`) auth or revoke in the break window; the ITV-identity gcloud in-window was read-only. Caught a **red herring**: the 09:46 `:3000` callback that looked like mise was a GA4 spike (`mint_token.py`, client `780647236756`) sharing the port.
- **Extended `mise-zikesa`** (the peer/dose-aware session's PKCE field report) rather than duplicating — corrected its cause (single-flow mint/persist → **cross-flow race** on the shared `.pkce_state.json`), added the off-box root cause, the **xrdp-wrong-account** finding, and a **rejected** dedicated-OAuth-client decision with rationale. All fields verified intact after the write.
- **Now-fix**: updated `~/.claude/context/environment.md` OAuth consent-dance section — on tube use the `--code` path; xrdp browser is on planetmodha so browser/listener flows barf.

### Reflection
- The spine was verify-to-root: a live refresh test settled "broken?", a session-transcript sweep settled "who did it?", and checking the actual `:3000` result (not assuming) killed the GA4 red herring.
- Sameer offered a hypothesis (a browser auth window barfing against planetmodha caused it). Tested rather than accepted/dismissed → it's a **symptom, not the cause** (mechanism: a wrong-account attempt on an Internal itv.com app can't touch the itv grant; timing: the one I could place was 12:24, *after* the break). But it surfaced the real actionable finding — the xrdp browser is on the wrong account.
- Self-caught a fail-open misread at the start (`ok:None` in the call log ≠ failure). Extended a peer's bon instead of minting a twin — the anti-duplication check paid off.

### Uncertain
- **Why Google revoked the token is unconfirmed** — it's off-box. Open question for Sameer: did anything touch `sameer.modha@itv.com` security on the morning of 2026-07-19 (password change / 2SV change / admin "sign out all sessions")? Any of those revokes all grants at once and would be the confirmed cause. If nothing, it stays "off-box, cause unknown."

### Risks
- The **two-flow PKCE race recurs** whenever two Claudes auth mise concurrently (fixes filed in mise-zikesa, not yet implemented).
- The **dose-aware session** (`1c25131f`) holds a stale revoked token in-memory — if it returns to mise (vs raw API calls) it needs a **restart** to pick up the fresh token.

### Opportunities
- **mise-zikesa** — implement the re-auth robustness fixes: (1) concurrency-safe `.pkce_state.json` (key by `state` param / lock / PID+ts) + a two-flow race unit test; (2) mint-time `challenge == base64url(sha256(verifier))` assert; (3) headless/xrdp steering to `--code`; (4) server re-read token.json on auth failure before declaring invalid_grant. It's a **suite-managed publish** (MCP change → needs a session restart to test) — deserves a fresh, focused session, not a tack-on.
- **mise-cebopo** still open (verify 2026 backfill completes, then remove the chunkBackfillAll trigger) — unrelated to this session; Sameer watching it live.

## For Claudes to come

- **mise token revocations are usually OFF-BOX / account-level, not a mise bug.** A security event on `sameer.modha@itv.com` (password / 2SV / admin sign-out) revokes *every* OAuth grant for the account regardless of client. Don't hunt for a code cause — re-auth and move on. (A dedicated OAuth client per mise would NOT protect against this — deliberately rejected in mise-zikesa.)
- **The robust re-auth path on headless/xrdp tube is the account-agnostic `--code` flow**: `uv run python -m auth` (print mode, no listener) → user clicks the URL in a browser signed into **itv** (their Mac) → paste the redirect back for `--code`. Tube's own xrdp browser is signed into `sameer@planetmodha.com`, so any browser / `xdg-open` / listener flow barfs (mit-workspace-mcp-server is Internal to itv.com). Now documented in `environment.md`.
- **Never run two mise auth flows at once** — they share `.pkce_state.json` and clobber each other's PKCE verifier (the confirmed cause of "challenge doesn't match persisted verifier"). Until zikesa's per-flow isolation lands, one flow at a time.
- **Diagnostic gotchas:** the mise call log (`~/.local/share/mise/calls.jsonl`) records `ok:None` for *successes* (a populated `result` = it worked) — don't read `None` as failure. And `localhost:3000` is shared by other tools' OAuth spikes (e.g. a GA4 `mint_token.py`) — verify the `client_id` before assuming a `:3000` callback is mise's.
