# Handoff — 2026-08-09

session_id: 07188607-ed57-4311-bbce-32f557ec08c4
purpose: The chips outcome closed whole, and calendar went from read-only to RSVP in one arc — four releases (1.46.6, 1.46.8, 1.47.0, 1.47.1), Sameer in the loop live
format: fond-v1

## For the next Claude

### Done
- Cleared the envelope debt from the previous session's three releases: `cues.gmail_url_context` and `chips_inserted` both green through the live 1.46.2 plugin. Minting the probe URL required writing the ENCODER for Gmail web tokens (the inverse of `_decode_gmail_web_token` — encode bare `f:<decimal>`, not `thread-f:…`, or the prefix gate refuses); filed as mise-hetaba (clickable Gmail web links in results).
- Closed mise-zikele: Calendar event-attachment write is API-feasible (current reference, worked example) but stayed unbuilt — scope + re-consent against zero demand. The 403 control was measured live, not config-inferred.
- Closed mise-kacani, shipped 1.46.6: single-tab sheet round-trips no longer upload the `=== Sheet: X ===` banner as row 1. `strip_sheet_header` lives in extractors/sheets.py BESIDE the writer it inverts, drift-guard test binding the two; `csv_text_to_values` moved out of ratchet-frozen create.py (927→922, baseline tightened).
- Closed mise-musepe, shipped 1.46.8: `include=` links now emit Gmail's own `gmail_drive_chip` markup (plain styled HTML — probed by raw-HTML diff of a native composer chip vs mise's links in the planetmodha mailbox). Delivery preserves it byte-intact; Sameer's screenshots confirm real Drive cards on web. Negative boundary recorded: the composer's recipient-access check is server-side ACL work mise can't replicate honestly — the skill teaches share-first.
- Closed mise-dibuje — the whole chips outcome, every surface written or ruled out: Docs writes (rafote), Slides total negative (gosadu), Calendar decided (zikele/gepiwe), Gmail compose-time markup (musepe).
- Filed AND closed mise-forunu same session: invite-email RSVP capability links are a measured negative — bare GET 302s to accounts.google.com sign-in, responseStatus stays needsAction; the tok pre-authenticates identity, not the write.
- Closed mise-gepiwe + mise-bozumu, shipped 1.47.0 then 1.47.1: Sameer chose the scope over a browser workaround; `calendar.readonly` SWAPPED for `calendar.events` (adapter is events-on-primary only, so all reads covered; ITV re-consented via --code flow); `do(respond)` is the 19th op — thread id or event id, accept/decline/tentative, read-modify-patch of the FULL attendees array (wholesale-replace semantics probed live before building), newest-invite-wins in threads (1.47.1), cancelled-meeting refusal, remote-excluded, teaching 403 for stale-scope tokens. Both routes live-fired on a throwaway event; Sameer watched the accept land in his own calendar UI.
- Suite 2,308 → 2,331; understanding.md synthesized at open (Request-union probe layer, staged-first watched-red controls) and updated through the day (chip round-trip measurement, Gmail chip grammar, the calendar one-day arc).

### Reflection
Probe-first won four times: the token-encoder roundtrip control, the wire-diff that revealed the chip grammar, the RSVP-link negative (baseline-read → GET → read-back), and the attendees-array wholesale-replace probe that shaped respond's write before any code existed. Sameer's presence turned costs into non-events — the scope re-consent was one click, and his calendar UI became the live read-back instrument for the RSVP probe.

One instrument lesson: the kube-Chrome scout wedged for minutes because `passe run`'s auto-wait never settles on Gmail (persistent channels defeat network-idle) — while `passe tabs` had the answer instantly (the goto had succeeded and landed on a sign-in redirect). For auth-state scouting, goto-then-tabs beats waiting for run completion.

Board mechanics: four publishes interleaved with other lanes' bumps (suite moved 1.46.2→1.47.1 with gaps at 1.46.7, 1.47.0 was ours) — the dry-run's version line, never the predicted number. The docs.py ratchet got its first argued raise (818→821): its mass IS resource text, so its ceiling tracks op additions; the baseline comment now says so.

### Uncertain
- Whether Gmail mobile renders the `gmail_drive_chip` markup identically to web — Sameer's "looks perfect" covered web screenshots; the markup is inline-styled HTML so mobile should match, but no phone screenshot was taken. Not worth chasing unless a report arrives.

### Risks
- Nothing shipped today has crossed a live `mcp__` envelope — this session's server was pinned at 1.46.2 throughout. The working-tree live-fires + installed-artefact diffs cover the substance; the envelope check is the next session's opening move (see Opportunities).
- mise-home's token still carries `calendar.readonly` — `respond` there returns the teaching 403 until a lazy re-auth. Deliberate; recorded in understanding.md.
- `respond`'s thread route reaches into `tools.fetch.gmail_attachments` for `_download_attachment_bytes` (lazy import) — a fetch-package refactor breaks it, but test_respond mocks that exact path so it reddens loudly.
- The four empty outcomes (wuralu, zohifi, wisuzu, jafide) were confirmed as intentional sketches at close — don't read their emptiness as neglect.

### Opportunities
- **Envelope check first (the standing reload rhythm): this session's fresh server runs ≥1.47.1, so one `do(draft)` with `include=` should carry `gmail_drive_chip` markup, and `do(respond)` should appear in the live tool description — two calls clear the whole 1.46.8/1.47.0/1.47.1 evidence debt.
- **mise-hetaba** — Gmail results carry a clickable web link: the encoder is written and round-trip-proven (recipe in the brief); wiring it into search previews and fetch metadata is a small, self-contained build.
- **mise-komibu** — comment triage arc, two actions left (suzomu live-exercise, rocosa posting-as-Sameer); the latter wants Sameer in the loop.
- **mise-riduka** — calendar clash-checking (query-mandatory limitation) is now the only calendar pain left on the board, and the scope swap didn't touch it.

## For Claudes to come

**When a Google surface renders something richer than mise emits, diff what the native composer puts ON THE WIRE before assuming a platform object.** The Gmail Drive chip — grey card, icon, the thing that looks like composer magic — is plain styled HTML: `class="gmail_chip gmail_drive_chip"`, inline styles, a public icon URL keyed by MIME type (`drive-thirdparty.googleusercontent.com/32/type/<mime>`). Gmail does no read-time upgrading (shared and unshared plain links render identically), so compose-time markup is the whole game, and mise can mint it. The instrument is cheap and repeatable on this estate: send the native artefact and mise's version to the planetmodha mailbox, pull both raw HTMLs via the mise-home token, diff — the two-tenant setup is a built-in A/B lab for recipient-side rendering questions.

**Record a negative WITH its surviving routes, and the negative becomes a decision menu.** mise-forunu's close note named three routes (scope + re-consent / logged-in CDP browser / stays manual); Sameer read it and picked the scope within the hour, and the whole RSVP feature shipped the same morning. A bare "doesn't work" would have parked the question; the route list turned it into a one-line decision. Same shape as zikele: "reopen on a real demand instance" met its demand instance eight hours later, and because the cost analysis was already recorded, nothing needed re-deriving.
