# Handoff — 2026-08-10

session_id: 2598afe2-a034-4ebc-84a8-d65e43ba8047
purpose: Two releases in one evening — the mypy ratchet (1.52.1) and the wheel-config fix that ends glaneur's nightly failures (1.52.2) — plus a sweep that turned six action-less outcomes into workable board items
format: fond-v1

## For the next Claude

### Done
- **Verified 1.52.0 through a live envelope on first contact** — a Gmail fetch returned `cues.people` (three ITV participants, full profiles), `cues.people_relations` with the user's own profile joining the arithmetic ("Sameer Modha is Stefano Figoni's manager"), and `cues.people_note`. Yesterday's top risk retired in two calls.
- **Closed mise-bunuvu, shipped 1.52.1.** `tests/unit/test_mypy_baseline.py` runs the documented mypy command inside the suite and ratchets per-(file, error-code) counts against an enrolled, reasoned baseline — new error reds CI naming the verbatim line; fixed error reds until the entry is lowered. Baseline chosen over fix-to-zero because errors here arrive from environment churn (image.py's pair appeared with the file untouched since March); route (a) — casting the http_client no-any-return cluster — stays open and composes (executing it trips the down-ratchet). Both controls narrow-reddened as predicted. CLAUDE.md's hand-count paragraph (wrong three times in a week: 16→18→20) replaced with a pointer at the baseline.
- **Swept the six action-less outcomes, verified each brief against code — none closable, board clean but under-actioned.** zohifi, jafide, dahune converted to standalone actions; wuralu converted and MOVED to the batterie-de-savoir board (bds-nigigi — assemble.sh lives in spm1001/batterie, its work is tracked as bds-*); wisuzu kept as outcome and gained mise-givige (a request-union probe, because "the Docs API supports tabs" is proven for read and UNPROVEN for tab creation); ditoja was a live fire — see next.
- **Closed the ditoja mechanism, shipped 1.52.2: glaneur.service had failed at 00:30 three nights running (Aug 8/9/10).** filters.py rode the wheel force-include, `config/attachment_filters.json` did not, so the first attachment-carrying matched thread killed the run. Fix: JSON joins force-include (asserted against the BUILT wheel by a new ~0.5s test); `get_filter_config()` degrades to a built-in mirror on FileNotFoundError ONLY (malformed JSON still raises); a sync test pins mirror == shipped file; test_wheel_closure's parser learned force-include now carries data files. glaneur reinstalled on tube (git-main dep, so the push delivered it); manual `glean --window 14d` ran the service's own job clean over the killing window: wrote=11, errors=0. mise-ditoja and glan-waroce (the infra census twin, which also names a tripped Healthchecks deadman "glaneur (tube)") both parked ⏳ on the 00:30 BST 2026-08-11 timer run.
- **Filed with rich briefs:** mise-wahane (both consumer doors walked in CI — converted to action at close, dispatch line added to the Toolmaking queue under the tell-after norm), glan-sawahi (a gleaned transcript knows where Sameer's own note for that meeting lives — deliberately a sketch until his meeting-note Shortcut design settles the join key), glan-ralife (glaneur's unpinned git-main mise dep re-resolves under background env rebuilds — decide the pin policy).
- **/open synthesis:** delta-gating method and the guard-placement lesson integrated into understanding.md; stale "gaseve carries this" line retired.

### Reflection
The sweep's verification-first shape earned its time: reading six briefs against code (five greps) found one item mid-list that was a live fire burning a nightly service, while also proving nothing on the board was rotten — both facts invisible from titles alone. The corpus paid for itself twice in real time: the wheel-probe false green (repo-cwd `python -c` resolving the working tree instead of site-packages) was caught in one step because understanding.md's import-divergence section was warm from the morning's synthesis; and mise's own teaching-error style meant the smoke correction cost one round trip. Also pleasing: Sameer's report to the receptionnaire and the infra alert-storm census had independently converged on the same defect from two sides (mise board + glaneur board) — the capture routes work, and the sweep stitched them.

### Uncertain
- Whether the glaneur *service* environment differs from tonight's manual-run environment in any way beyond the fixed config path. Auth provably works there (waroce's traceback reached filter-time, which is post-auth), so the residual is small — but the manual green is my shell, not systemd's.

### Risks
- **If the 00:30 run reds again, it is a NEW fault, not the fix failing.** The config path is cleared end-to-end (installed-env probe from neutral cwd + full manual run) and service auth is proven. Two simultaneous faults disguise each other — diagnose fresh, don't re-litigate 1.52.2.
- **The mypy baseline WILL churn when Dependabot bumps mypy or type stubs — that is the design, not flakiness.** The failure message teaches the adjudication (fix, or raise the entry with a reason argued in the commit). Expect it mid-unrelated-work, same as the guidance-freshness prefix reds.
- CLAUDE.md's pytest count reads 2331 against an actual 2414 — the paragraph is dated and self-describing; correct it inside whichever next publish touches CLAUDE.md, not as its own bump.
- 1.52.1/1.52.2 were not re-verified through a live MCP envelope; deltas were docs-only and wheel-packaging-only (the plugin vendors full source, so config/ was always present on the MCP path). Low, named for completeness.

### Opportunities
- **Tomorrow's first move: read `journalctl --user -u glaneur.service` after the 00:30 BST run** — a green Finished closes mise-ditoja AND glan-waroce (cd to glaneur for that one), and the "glaneur (tube)" Healthchecks deadman should recover on its own.
- **mise-wahane is fully briefed, converted, and dispatch-queued** — the library-door test reuses tonight's wheel-test shape (build, clean uv venv, core-only install, import sweep from NEUTRAL cwd) and is about an hour with controls.
- **mise-givige is a 15-minute opener** — one union grep + one live probe settles whether mise-wisuzu is buildable at all.
- mise-riduka (calendar clash-checking) is now the biggest untouched pain for the fifth handoff running.

## For Claudes to come

**A test suite's greens are scoped by which door the tests enter through — enumerate the product's doors and give each its own walk-through.** mise has exactly three: the MCP envelope (Claude sessions over stdio), the installed wheel (glaneur as a library; Cornichon's slim vendor is the same door with fewer extras), and — beyond CI's reach — the marketplace-installed plugin. Both wheel burns (mise-dopufo's five unshipped modules, mise-ditoja's missing data file) happened under a fully green suite, because every test entered through the repo's own sys.path and so stood where no consumer ever stands. The exposure compounds by an asymmetry worth naming: the least-attended consumer rides the least-contractual surface — MCP breakage is seen by a human-attended Claude within minutes, wheel breakage fails at 00:30 in a journal behind a deadman email. So guards for the quiet door must be artefact-level (build the wheel, install it, enter through it), not static. Two craft corollaries from the same evening: a `python -c` probe inherits its cwd as sys.path[0], so probing an installed environment from the repo root silently tests the working tree — probe from a neutral directory, and treat a suspiciously familiar path in the output as the tell; and this repo's house move for standing-debt-nobody-can-verify problems is now three instances strong (module sizes, guidance negations, mypy errors) — freeze the present in an enrolled, reasoned baseline, police the delta both directions, and make every red teach its own adjudication.
