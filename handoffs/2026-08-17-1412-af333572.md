# Handoff — 2026-08-17

session_id: af333572-c5d4-43a1-b746-4248557dffbc
purpose: spec-2 double-dispatch caught and stood down + the format bench built, controlled and committed + the discrimination gate run and read + an Opus taster that priced the estate's one real routing decision
format: fond-v1

## For the next Claude

### Done
- **Stood down from a live spec-2 collision with zero duplicate cost** — the wave-2 dispatch lane (`~/scratch/pilot-wave2-2026-08-17/spec2/`) was already executing session spec 2 when this session went to build it; caught at setup, before any spawn. Rolira's tactical step 1 skipped with the lane named; LANE NOTE on the brief; catch-up note for the orchestrator at `~/scratch/note-to-wave2-orchestrator-2026-08-17.md`. Their verdict landed: **routing hints work at eye level (README +40pp), not in manifest.json (read 15/20, obeyed 0/10)** — `~/notes/practices/mise/routing-findings.md`.
- **Built the deposit-format bench and completed rolira step 2** — `tests/bench/` (commit d065709, pushed, CI green): seeded generator (5 slices, 66 Decimal-exact questions), 7 format renderers, independent verifier, idempotent ardoise runner, mechanical scorer. Every control fired before anything was trusted: perturbed answer key reddened exactly its one check (287/1), scorer self-test 6/6, live smoke caught two classifier bugs (jq is querying; cat-of-manifest is not a whole-read).
- **Ran the discrimination gate** (87 spawns, $60.35, Fable 5): accuracy at ceiling everywhere — 60/60 intact, 18/18 sabotaged, 18/18 byte-exact quotes. What separated: **format recruits method** (json-min → querying 7/10; aligned/md → grep+programmatic) and **inline cost** (4–6× agentic; json-min inline +64% over aligned). Path variance measured at zero (9/9 identical). Full verdict + league redesign: design doc §Gate run results.
- **Ran an Opus 5 taster** (19 runs, $9.56) on Sameer's ask: 19/19 correct, 3/3 sabotage runs **disclosed the corruption**, format-recruits-method replicates, and Opus ran identical cells ~30–50% cheaper with no turn-count penalty — inline exactly half Fable's price.
- **Decisions recorded**: two-folder workspace pattern blessed (vehojo — Drive corpus folder + Outputs/ subfolder; NO properties lane; Garni's board told via agsp-somiho); reader panel = real consumers only (Fable 5, Opus 5, Gemini 3.7 Flash with thinking — Haiku/Sonnet dropped); replication = 3 distinct instances per cell, not identical reruns.
- **Board/estate hygiene**: mudupa + tefihi converted from empty outcomes to standalone actions; trousse-fawufi filed (ardoise print-mode cwd hardcode — two lanes patched it independently in one morning); duckdb 1.5.5 + polars[rtcompat] 1.43.2 installed into tube's system python (needed by bench subjects; polars standard wheel illegal-instructions on tube's no-AVX2 CPU).

### Reflection
The day's shape was collisions handled by records: the double-dispatch was caught because setup tripped over the other lane's fixture, and every seam got written down where the next reader would look (skip note, lane note, orchestrator note) — which is why the "am I muddled?" audit later reconstructed cleanly from disk. Sameer's three design pokes each subtracted something the bench didn't need (identical reruns, a phantom cheap-model audience, a already-measured format arm); the gate then vindicated the replication cut with a measured zero. The one vocabulary failure: "pilot" collided with his pilot-wave programme and cost a round trip — renamed to "gate run".

### Uncertain
- Whether Gemini 3.7 Flash (thinking on) holds the accuracy ceiling — the league's central question; every discrimination hope now rests on the weak-end reader, the inline arm, the harder slices (S2/S4/S5, never gated) and the 50k tier.
- Whether the ~50-minute background-task ceiling is real — the gate run finished in ~45 min and never tested it; resumability is designed in regardless.

### Risks
- **`~/bench-work/` is the live bench data area** (fixtures regenerable; transcripts/results are measurements — now archived to `~/notes/practices/mise/format-bench-2026-08-17/`). Don't tidy it while rolira is open.
- **CLAUDE.md has no tests/bench/ pointer and must NOT get one as a standalone commit** — CLAUDE.md is vendored; an unbumped edit wedges the marketplace ratchet. The line rides step 4's (or mitoki's) publish; noted on rolira's brief.
- The scorer's transcript parser is CC-stream-json-specific; the Flash arm needs its own runner + parser (plain API reader — simpler, but a real seam).
- The gate ran S1 only: "a tooled top-end reader cannot be failed" is proven for long-clean-table shapes; S2 wide / S4 buried / S5 join could still dent even Fable — don't over-generalise the ceiling.

### Opportunities
- **Rolira step 3 is current: the format league** — Gemini 3.7 Flash runner first (mine Garni's repo for its consumption pattern + thinking config), then census-weighted league over the harder slices with foregone cells dropped; dispatch line added to the Toolmaking queue. Design + league shape: `~/notes/practices/mise/format-bench-design.md`.
- **mise-vehojo is now a documentation errand** — write the two-folder pattern into the library docs and close; the decision and rationale are on its brief.
- **mise-mitoki (pdftotext swap) can proceed in parallel with the league** — the census verdict is fidelity-complete; only structure-preservation questions wait on rolira's bench.
- Two Todoist lines are tickable by Sameer (spec-2 — delivered by the wave; vehojo's one-folder-vs-two — decided) — flagged in the close ceremony, his hand on the phone.

## For Claudes to come

**Design evals for the reader you can still fail.** A tooled frontier reader (Fable 5 AND Opus 5, measured on 105 runs today) cannot be failed by clean or even sabotaged long tables at 2k–10k rows: handed a fixture whose every header lies, both models detected the scramble unprompted, remapped columns from the data, answered exactly, and named the corruption to the user — detect-recover-disclose as a top-tier property. Consequence: accuracy rows for strong-tooled readers are foregone 100%s, so eval spend belongs where failure is still possible (weaker readers, no-tools inline delivery, harder shapes, scale past context) and the discriminating axes at the top end are cost and method-recruitment, not correctness. Two companion findings with policy weight: **the file format is itself a routing hint** (json-min recruited both models into the query lane ~6/10 vs near-zero for aligned/markdown — a third hint channel beside spec 2's README-beats-manifest), and **inline delivery pays cache-WRITE prices while agentic pays cache-READ discounts** (4–6× per run, and format token-differences that are invisible agentically become +64% real money inline) — so format economics matter most for exactly the unattended consumer nobody watches.
