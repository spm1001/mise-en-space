# Handoff — 2026-08-19

session_id: 5bdb54d5-c7cd-42de-856e-7f008d1e2b02
purpose: build + ship the calendar-write ops (mise-rijeco), then dry-run the hardest real scheduling puzzle against them
format: fond-v1

## For the next Claude

### Done
- **Built and shipped mise-rijeco's three ops in suite 1.68.0**: create_event (blast-radius confirm gate, invite-first sendUpdates=all), update_event (structural gated / cosmetic quiet, cues.previous as the undo reference), freebusy (busy + mined common slots + workingLocation office days, not_visible excluded loudly). Morning probes shaped it: freebusy 403s on the events scope (calendar.freebusy added; Sameer re-consented same day — the token is live, proven with a real query), RRULE patch converts a single to a series, attachments enrich fileUrl→fileId, DTSTART outside BYDAY survives as a stray instance (tool warns), user timezone resolves from their own diary. 46 new unit tests (suite 2,580), smoke_stdio 10/10 including a create_event preview over real MCP, CI green both publishes.
- **Shipped 1.68.1 same day**: the freebusy timezone render fix (first live output mixed +01:00/+00:00 offsets in ONE slot — read as zero-length; caught by reading the output, fixed, pinned by a uniform-offset test). Publishing it also cleared the release-ratchet quarantine that tomorrow's 07:00 cron would have fired on vendored-content drift. CLAUDE.md calendar rows (adapter surface + Key behaviors bullet) rode along — NB guidance edits are session-cached, so this session never saw its own CLAUDE.md changes.
- **Closed the 1.67.0 envelope risk from the morning handoff**: first live PDF fetch returned extraction_method pdftotext.
- **Dry-ran the 1:1 programme end-to-end with Sameer (rijeco acceptance test ④, zero writes)** — the full settled spec (grid, three alternating pairs, bridges, strip list with organiser mechanics, GTD-block move, room emails with freebusy evidence) is banked on mise-rijeco's --how. Step 5 remains: a fresh session runs the allocator (mine 15/22 Sep first — unmined), stages everything through preview gates, one approving pass. Queue line added.
- **Filed the capability crop the puzzle exposed**: mise-wavotu (colleague-diary detail lane — the big one), mise-reboto (calendar URL resolution), mise-gujiro (extendedProperties stamping — the reconciler's join key), mise-kawegu (colour/labels), mise-pogotu (room directory probe + resource-attendee booking), mise-nobodo/mise-redewa (the drift-reconciler outcome + intent-file first action).

### Reflection
The session was two halves and the second validated the first: ship in the morning, then Sameer deliberately fed it "the hardest puzzle to make sure the tooling works the way you need". The tooling held (one freebusy call for ten calendars, honest not_visible, previews meaning zero risk all day) and its sharpest gap surfaced within hours — freebusy answers "occupied?" while every load-bearing judgement needed titles, reachable today only via repo access. Sameer's soft questions were better calibrated than my instruments twice: "can you see the detail?" caught me attributing his own Review block to Ella, and "Stef's 8am is clear-inbox" caught the soft-block blindness. The ratchet system also had its best day: the description budget forced detail into mise://docs (by design), and resources/docs.py's baseline comment argued down my own split-the-module reflex — I reverted to the recorded decision. His "this is becoming a great adventure" is worth carrying: the collaboration pattern (machine holds constraint arithmetic, human holds soft knowledge, preview gate is the seam) was articulated BY HIM mid-session ("because you're properly intelligent, you can figure things like that out — diary stuff needs judgement").

### Uncertain
- Whether Admin SDK resources.calendars.list has a non-admin view (mise-pogotu probes it — don't assume admin-gated; that assumption cost six months on the staff directory).
- Whether "Neil Charles" genuinely has zero meetings with Sameer in 3.5 months (calendar q search returned zero; plausible for a contractor, un-cross-checked).
- Steady-state odd-Tuesday availability for Lauren's slot — 15/22 Sep were outside the mined window; the anchor is provisional until execution mines them.

### Risks
- **The programme is designed but NOT booked** — every old 1:1 series still stands. If a session "tidies" calendar items before execution runs, the strip list in rijeco's --how is the only complete record of what must go vs stay (Dan's TVA series and the Tue 13:30 Lauren/Ella/Sameer threesome stay).
- The execution session must re-check room freebusy at booking time (rooms churn) and read each room's responseStatus in the create read-back — rooms auto-decline on conflict, silently from the caller's view.
- mise-gujiro (stamping) ideally ships BEFORE the booking run so the programme is stamped from birth; if booking goes first, backfill stamps via update_event.
- Sameer's GTD blocks move to 08:00–09:00 as part of execution — until that lands, the grid's 09:00 slots look clear but his 08:30–09:30 Review blocks still sit there.

### Opportunities
- **Execution of the 1:1 programme is fully staged (mise-rijeco step 5)**: fresh session, draw down rijeco, mine 15/22 Sep, allocator over the banked spec, stage eight series + two bridges + strips + room attendees through preview gates for one approving pass — the queue line points here.
- **The capability/eng discussion Sameer felt was missed**: the six filed items (wavotu, reboto, gujiro, kawegu, pogotu, nobodo) ARE its agenda — a session walking those with him as a set would settle the calendar roadmap in one sitting.
- **mise-gujiro + mise-kawegu batch naturally with the next publish** (small params, shared budget check); wavotu is the design-heavy one and earns its own session.
- mise-hihohu (smevals in banc), mise-buliwe (Gmail-attachment crops) and mise-gogovi (showpiece charts) carry over unchanged from the morning handoff.

## For Claudes to come

**Calendar has two instruments and the split is judgement-shaped: freebusy answers "occupied?", only the detail lane answers "by what, whose, and does it bind?"** Four worked cases from one afternoon, all invisible to busy-blocks: a colleague's 08:00 'Clear inboxes' is opaque-busy yet bookable (soft block); a 9-to-5 titled with the user's own name was a Front-of-House ROOM hold (the machine tell: organiser ends `@resource.calendar.google.com`, creator frontofhouse.iwc@ — classify as non-human busy); the user's own conference was marked TRANSPARENT (free to freebusy, humanly absent — every mined slot on those days was fiction); and a dark day was all-day 'AL' (leave, not meetings). The merged-gaps method has a fifth failure of its own: it destroys attribution, and this session sold the user's own Review block back to him as a colleague's constraint. Corollary the user named himself: the diary is also an empirical PERMISSION record — rooms he organised and the room accepted are self-bookable; rooms only ever created by FoH are gated, and the boundary tracked capacity exactly.

**When a size ratchet blocks you, read the baseline's comment before reaching for the obvious remedy — the comment may be spec against exactly your move.** resources/docs.py went over its frozen baseline; the trained reflex (split into a sibling, the house's own "ratchet as design force" pattern) was already executed when the baseline's comment surfaced: "this module's mass IS resource text; splitting a resource string across siblings would be worse; raise only with a new op." The split was reverted and the number raised with an argued reason. Both moves are house patterns; the in-file comment is what picks between them, and it only picks if read BEFORE acting. Same family as verification.md's in-file-comments-are-spec, sharpened: the comment nearest the failing number outranks the pattern-match from the last success.
