# Handoff — 2026-08-23

session_id: 5d5cf540-4f15-5fe7-8bc9-2ce3b2537d0d
purpose: mcp 2.x reccy → migration shipped (1.71.0) → v2 opportunity scan → elicitation probe → wamoco bug caught and repaired → fail-open guards shipped (1.71.1)
format: fond-v1

## For the next Claude

### Done
- Ran the mcp 2.x reccy Sameer asked for: prior decisions (mise-veraja, mise-kofoba) re-read, what-changed established (2.0.0 shipped 2026-07-28, v1 security-fixes-only, roadmap 2026-08-22), contact surface inventoried, spike branch proved the port at five lines with the suite green (spike/mcp-2, kept as evidence).
- Shipped the migration as suite 1.71.0 (mise-vucufu closed): FastMCP→MCPServer, tool bodies serialized via a `_serialized` threading.Lock (the anyio limiter cap DEADLOCKS stdio — transport shares the pool; recorded in CLAUDE.md + test docstring). Verified three ways against the installed artefact. Dependabot PR #25 closed itself; provenance comment added.
- Probed elicitation end-to-end (mise-huwubi candidate 1, CONFIRMED): CC declares the capability, renders a real form dialog, the model provably cannot see or supply the answer; headless auto-cancels cleanly. Era-neutral Resolve(Elicit) pattern proven against today's CC.
- Caught and repaired a silently-dead-since-birth feature (mise-wamoco closed): v1 ran sync tool bodies on the loop thread, so asyncio.run() in gmail_browser/cdp always raised into fail-open excepts. Three-arm experiment: direct call resolves, v1 envelope silently refuses, v2 envelope fetches the thread. First production success of thread-a resolution ever.
- Shipped the class-level guards as suite 1.71.1 (mise-pagigo closed): fallbacks disclose why they fired, undesigned failures reach the log (cdp, genai, doc-chips restore path), async_bridge is the one door for async-from-sync (grep-test enforced, hostile-environment test with built-in control).
- Board hygiene per Sameer: mise-hezimu/tesuto/taciku converted to standalone actions, area "future developments", parked Someday with wake conditions. Corpus lessons filed to carte intake (carte-wenele: fail-open/fail-loud+disclosure rule; carte-jisavu: gh run watch empty-id false red). Probe artefacts preserved in docs/research/2026-08-23-mcp2-probes/.

### Reflection
The session's shape — reccy, then Sameer's challenge ("is it safe to move" vs "what does the move make possible") turning it into an opportunity scan, then the scan paying for everything — is worth imitating. Two design reversals happened on evidence mid-flight (limiter→lock; serialize-as-destination→serialize-as-sequencing) and both improved the result. The false-CI-red near-miss (empty run-id → usage-error exit read as red) was caught by re-deriving before verdict; the instrument's-limits reflex earned its keep twice today.

### Uncertain
- The exit-144 death of one background task during the deadlock diagnosis: the incident is real, but my in-flight explanation (pattern-match on the harness wrapper's cmdline killing its own shell) doesn't fully survive re-derivation — the bracket-trick regex shouldn't have matched the wrapper's bracketed string. Mechanism unresolved; the robust practice (kill by exact PID from prior output, never by pattern) stands regardless.
- genai/cdp cookie fetch shares wamoco's mechanism and is repaired by the same change, but was NOT separately live-probed (needs a chrome-debug with GenAI access).

### Risks
- Wheel consumers (glaneur, Garni) inherit the mcp 2.x dependency tree at their next build. The library tier imports nothing from the SDK so it should be inert — their next nightly run is the evidence, not our grep.
- Sessions started before 2026-08-23 keep their spawn-time mise server silently (bds-sawalu); anything odd reported from an old session, check its version first.
- The serializer lock means tool calls still queue one-at-a-time inside the server — if someone reports "parallel mise calls are slow", that's the designed state until mise-bapije, not a regression.
- spike/mcp-2 (origin) is a superseded design (no lock); anyone resurrecting it must re-read mise-vucufu's closing note first.

### Opportunities
- mise-bapije: lift the tool-body serializer — bounded thread-safety audit (four named targets), delete the lock, measure parallel multi-fetch before/after. Sized for its own release.
- mise-huwubi: elicitation is proven — the confirm-gate redesign (do(share)/create_event asking the HUMAN via the client UI, confirm= kept as fallback for capability-absent clients) is ready to become a real design; remaining candidates (OTel latency, server instructions, output schemas, version stamp) each need one probe.
- The calendar booking run is still fully armed from the previous session (mise-rijeco staging, tactical mise-gogovi parked with progress kept — resume with `bon work mise-gogovi`); this session deliberately left it untouched.
- carte-wenele and carte-jisavu await corpus-intake adjudication on the ~/.claude board.

## For Claudes to come

A version-pin assessment has two halves and the second is where the value was: "where do we break" (prior decision → what changed since → contact-surface inventory → migration guide read against the inventory only → reversible spike) and "what assumptions can we now revisit" (the opportunity scan). The first half took an evening and found a five-line port; the second found a protocol-native confirm gate, a feature that had been silently dead in production since birth, and a class-level guard now shipped. When a human asks "what does moving entail", deliver both halves — the safety scan alone answers a smaller question than the one they need answered.

The wamoco class, for recognition: an environment-sensitive call + a catch-all fail-open + an undisclosed fallback = a feature that can die invisibly while every direct test stays green. mise now guards it mechanically (async_bridge + grep-test + disclosure), and the estate-level rule is in carte intake. The related craft fact with teeth: mcp 1.x ran sync tool handlers inline on the event loop; 2.x runs them on worker threads — concurrency, thread-affinity, and asyncio.run behaviour all changed in that one line of SDK dispatch, and none of it appears in a tool's own diff.

## Addendum (post-close discovery, same session)

The close-time drift check found that **session handoffs were shipping to the public marketplace**: the visible-handoffs migration (bon-sanita, two days earlier) moved them out from behind the assembler's `/.bon` exclusion, and no rule caught the new path. mise's whole `handoffs/` and sonnette's went public from suite 1.70.0; mise's root `HANDOFF.md` (2026-04-09) for longer. Fixed same hour: `assemble.sh` now excludes `/handoffs` and `/HANDOFF.md`, suite **1.71.2** re-assembled with `--delete-excluded` sweeping every vendored copy, purge verified at marketplace HEAD and in the installed cache. The remaining decision — the public repo's git *history* still holds the exposed bytes — is Sameer's, filed as **bds-dicibe** with a recommended read-through-then-decide path. Lesson for the estate (candidate for bon-sanita's own repo review): when a migration moves files across an exclusion boundary, the exclusion list is part of the migration.
