# Handoff — 2026-08-24

session_id: a1ed3756-012f-4700-b65c-282b01a26df1
purpose: dispatched worker (& Toolmaking queue) — author the remaining Workspace-MCP bench hiding-place fixtures under mise-zidipo
format: fond-v1

## For the next Claude

### Done
- Filed and completed **mise-resubu** (action under mise-zidipo): all three authorable hiding-place classes went open → authored-and-scored-and-control-tested in one session. The corpus (planetmodha My Drive, folder `1XpaeAXMvYrr30U_TD7a8xHNuxHM1Uc8v`) now holds four fixtures, 15 real probes, 20+ control probes.
- **FIXTURE-02** (multi-tab sheet, `1cnNI80r6xkuZf7JPDkIhEx_uAOInw5OEWmxT4XMLqUM`): tab-2 facts contradict tab 1 ("the Overview tab is stale"). Authored through mise itself via the mise-dahune multi-tab deposit route. Scored 3/3. mise-suwopu bit again exactly as filed — create cue claimed both tab names landed, read-back showed tab 1 kept the spreadsheet title (Drive minted sheetId 2078320758; second instance appended to that report). Tab 1 renamed to "Overview" post-create via adapters/sheets.py with `MISE_TOKEN_PATH` pointed at the home token; deposit re-fetched afterwards so the corpus is self-evidencing.
- **FIXTURE-03** (Slides deck, `1ls9Ry93qFOE1hVh7rp_WSBLZIYfPUhuVT--3pwxua9A`): speaker notes admit the £118,400 overrun the slide body denies; a chart PNG carries "71.9%" as pixels only (verified absent from every pptx text layer before probing). Scored 4/4 including vision-only disclosure via `slide_03.png`.
- **FIXTURE-04** (Gmail thread `1a0316901e2f93e2` + PDF attachment): email body asserts the stale 46,908 as current; the attachment supersedes it with 46,871 plus a fee stated nowhere else. Route: source Doc → Drive PDF export → Gmail `messages.insert` with `internalDateSource=dateHeader`; source Doc trashed so the facts are attachment-only. Scored 3/3, `found_in` cleanly separating body (content.md) from attachment (`.pdf.md`). Thread archived + marked read so it doesn't sit in Sameer's real inbox — the README says not to "tidy" it.
- **Speaker-notes verdict, measured** (was grounded-but-untested): pandoc → pptx (`::: notes` divs) → Drive upload with target mimeType `application/vnd.google-apps.presentation` converts to a NATIVE Slides deck; notes survive conversion; mise's fetch extracts them under `### Speaker Notes`. mise still cannot *create* decks (mise-larego stays true; the measured route is appended to its brief for when demand wakes it).
- **Scorer widened** (fact_recall.py): artefact discovery now scans `content.md`, `comments.md`, other `*.md` (catches `.pdf.md` attachment extractions), and `*.csv` (catches per-tab sheet deposits). Per the corpus rule, the WHOLE control suite was re-run against the widened scorer: original CONTROL set holds row-for-row on FIXTURE-01 (still 5/5 real), plus new CONTROL-02/03/04 siblings all seen failing as designed — including a deposit-level known-bad (a `thumbnails=False` fetch of the deck flips the vision-only detector to NOT-disclosed, so its green is a tested green). Full sweep + addendum: `~/notes/practices/mise/bench-corpus/runs/2026-08-24-authoring-run.txt`.
- **Cold-review pass before the done verdict**: a fresh-context refuter attacked the done-criteria with the artefacts and reproduced all seven scorer runs independently. No refutation landed. Its two caveats were both acted on: the any-PNG weakness is documented in the README class table AND filed as **mise-dunama** (tie the detector to its exhibit + wrong-PNG control before comparative use); the pre-rename FIXTURE-02 deposit was re-fetched and re-scored (invariant).
- Board hygiene: living-log append on mise-zidipo; measured notes appended to mise-suwopu (second sheetId instance) and mise-larego (route retired the engineering risk); suggested-edits class recorded as PARKED (platform-verified — no create-suggestion endpoint) with unpark conditions in the README.

### Reflection
The dispatch's staleness delta was right and saved the session: the "probe the sheet-tab block first" clause was already answered on mise-dahune, so the whole session went on authoring rather than re-proving. The probe-before-workaround discipline paid again on speaker notes — ten minutes of pandoc+upload converted a borrowed "blocked" into a measured "open" and killed a planned workaround. Control-first scoring meant no first-run green was ever trusted naked; the one instrument weakness that survived to review (any-PNG disclosure) was caught by the refuter precisely because its brief said "find the check that could never fail". A mid-session stall (Vertex blip, ~2h, one Bash round lost) cost nothing but time — everything banked before it survived, and re-running from disk state was clean.

### Uncertain
- Pandoc smart-quoting: CONTROL-03's typography row rests on pandoc 3.1.11.1 turning a straight apostrophe curly. A different pandoc or `--no-highlight`-style flag set could change that; the control would then need re-basing (it would show as an unexpected verbatim HIT, which the README's expected-results list would catch).

### Risks
- The scorer's relaxed match is substring-over-folded-text: a probe value that is a digit-run could in principle match inside a longer number (refuter checked all current deposits — no collisions today). Worth re-checking when new fixtures add values.
- `fixtures-src/` scripts embed live IDs (FIXTURE-04's insert_email.py carries the now-trashed source Doc id) — re-running them blind will fail or double-insert; they are records, not tools.
- The corpus deposits under the repo's `.mise/` are ephemeral evidence — the run file names them, but a `.mise/` clean would orphan those references. The probes + fixtures in Drive/Gmail are the durable ground truth; deposits are re-fetchable in minutes.
- FIXTURE-04's thread lives in a real mailbox: a zealous inbox-triage session could trash it. The README carries the don't-tidy warning; the fixture is re-insertable from fixtures-src if lost.

### Opportunities
- mise-nihebo is now the corpus's only structural gap: the private ITV twin for Shared Drive paths (deliberately untouched this session per the dispatch).
- mise-dunama tightens the vision-only detector (exhibit-tied, wrong-PNG control) — required before the class appears in any cross-server comparison; fine to defer while the bench is mise-diagnostic only.
- The bench's next real gate is unchanged from the zidipo brief: comparator wiring and the within-mise skill-file ablation (design note v0.3), now with a complete read-only corpus to run them against.
- The & Toolmaking queue line "author the Workspace-MCP bench fixtures … (mise-zidipo)" is tickable — this session was its dispatch (dispatcher ticks; Todoist deliberately untouched).

## For Claudes to come

The corpus cannot be authored entirely through the tool under test — now measured, not assumed, with the split on record: of the five hiding-place classes, one authored through mise end-to-end (multi-tab sheet, minus a tab-1 rename), two needed raw APIs beside mise's own credentials (Slides via pandoc→pptx→Drive-convert-upload; email via Gmail messages.insert — do(draft)'s include= is Drive links, not attachments, and a draft is not a received thread), and one is impossible for any API (suggested edits). The reusable authoring facts: `MISE_TOKEN_PATH=<flavour token> uv run --extra extraction python` gives a script mise's own authenticated client (adapters/http_client.get_sync_client), which covers Drive convert-uploads (multipart with target Google mimeType), Drive PDF export, Sheets tab surgery, and Gmail insert with one import. And the control discipline that earned its keep twice: when a detector's pass condition is an existence check (any PNG, a line prints, a file exists), author a deposit-level known-bad that makes it fail before trusting any green — the vision-only detector's zero-PNG control passed that test, and the cold review still found the wrong-PNG mode it couldn't cover, which is exactly the kind of orthogonal-check hole existence checks breed.
