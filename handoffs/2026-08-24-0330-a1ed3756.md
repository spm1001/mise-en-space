# Handoff — 2026-08-24 (second close, same session as 0246)

session_id: a1ed3756-012f-4700-b65c-282b01a26df1
purpose: dispatched follow-on cards after the bench-fixtures close — mise-huwubi (v2 opportunity verdicts) and mise-bapije steps 1-4 (serializer lift), worked serially with per-card refuters
format: fond-v1

## For the next Claude

### Done — card 1 (mise-huwubi, CLOSED)
- All eight v2-surface candidates now carry probe evidence + a verdict; none survives as vibes. Adopts filed as four items: **mise-wagina** (elicitation confirm gate, outcome — pilot on share with confirm= fallback), **mise-gojoku** (OTel per-call latency — the SDK's middleware is ON by default, a wired TracerProvider measured a 120ms body as a 121.3ms span), **mise-fowabo** (server instructions — CC injects them under an "MCP Server Instructions" heading, canary-verified headless and corroborated interactively), **mise-vubeku** (version stamp + public-tool-API hygiene pair). Parks recorded as chosen in CLAUDE.md "Two v2 capabilities are deliberately NOT adopted": typed output schemas (CC gives the model ONE compact-JSON text block — mechanically verified via stream-json by the refuter, upgrading my weaker self-report probe; schemas would grow tools/list on mise's margin axis) and errors-in-band (teaching errors are FOR the model). Candidate 8 (remote deposits) parked pending mise-mawawa. New measured fact for zidipo's re-harvest: the 2.x migration auto-minted ~75-byte permissive outputSchemas on all three tools (noted on mise-zidipo).
- Probe artefacts committed: `docs/research/2026-08-24-huwubi-probes/` (in-process wire probe answering C2/C3/C4/C5 in one run; CC-side canary server + config; README mapping file→finding).

### Done — card 2 (mise-bapije, steps 1-4 BANKED, released at step 5)
- **The `_serialized` tool-body lock is deleted; tool bodies run concurrently.** Guards live where the state lives (all on main, two commits): per-resource deposit lock in `workspace/manager.py` — taken by the fetch dispatch, by `fetch_gmail` a second time on the RESOLVED thread id, and by every do(`source=`) reader + `enrich_manifest` (keys converge on the manifest resource id, pinned by test); O_EXCL search-deposit naming; single-flight refresh + locked singleton mint in `http_client.py`, identity caches cleared BEFORE the dead-grant credential swap.
- **Release-note number, measured on the final code through the real stdio server: parallel 3-fetch 5.14s → 2.37s median (2.2×)**, wall tracks the slowest fetch, deposits intact every rep. `CHANGELOG.md` carries an `[Unreleased]` section ready for the version stamp; full record `docs/research/2026-08-24-bapije-concurrency/RESULTS.md`.
- Every guard test was SEEN TO FAIL with its guard removed before its green was trusted (old exists()-loop 20/20 collisions, unlocked stampede 8 refreshes, unlocked router arm interleaving, 8-thread mint burst).
- Rode-along fixes: the pre-existing guidance-freshness red (calendar_id negation from the mise-wavotu ship) enrolled as adjudicated-true; three size ratchets raised with argued in-table notes (+11 create, +8 gmail, +34 http_client total).

### Reflection
The per-card refuter earned its keep asymmetrically: card 1's mostly upgraded evidence quality (self-report → mechanical stream-json), card 2's REFUTED the audit's central negative three ways — do(source=) unguarded, message-id/thread-id lock divergence, unlocked singleton mint — and all three were fixed, tested, and re-measured within the hour. The lesson that generalises: a thread-safety audit scoped to "the fetch path" missed that do() touches the same folders; the refuter found it because its brief said attack the negative with the code, not reassurance. Also the guards cost nothing measurable on the parallel path (2.36→2.37s), which is the design working: locks sit exactly where collisions are, nowhere else.

### Uncertain
- The cold review noted CC re-serializes tool results to compact JSON, which *suggests* it consumes structuredContent internally — unproven either way; the CLAUDE.md park trigger is worded on what the model receives, which is the measurable quantity.

### Risks
- **mise-bapije step 5 (publish) is deliberately NOT run** — the queue loop forbids publishing. Until the next `/batterie:publish`, the installed plugin still carries the serializer lock and none of this session's code; the working tree and main are publish-ready. The CLAUDE.md edits also ride that publish (vendored content).
- CLAUDE.md changed twice this session (v2 non-adoptions block; serializer paragraph rewritten for the concurrent world) — guidance is session-cached, so sessions started before the edit read the old text.
- Cross-process deposit races (two servers, one cwd) remain out of scope — same as under the old serializer; the router comment says so.
- `_DEPOSIT_LOCKS` grows one RLock per distinct resource id per process lifetime — bounded by session usage, no eviction; noted as benign by the cold review.

### Opportunities
- mise-bapije needs only step 5: `/batterie:publish` from this tree, then re-verify against the installed artefact (`bon work mise-bapije` resumes at step 5; the banked release note is in CHANGELOG `[Unreleased]`).
- mise-wagina (elicitation gate) is the highest-value adopted candidate and is design-ready — the probe evidence and fallback semantics are all in its brief.
- mise-gojoku, mise-fowabo, mise-vubeku are each small and could share a release with the bapije publish.
- The zidipo Tier-0 re-harvest note (auto-minted outputSchemas) is waiting in that brief for whoever re-runs the token table before publishing bench results.

## For Claudes to come

When lifting a serializer, audit by asking "who touches this state" rather than "which path was I working on" — the lock was on tool BODIES, so every op family shared its protection, and the audit inherited the fetch-centric frame from the brief while do(source=) touched the same folders unguarded. The refuter caught it only because its brief targeted the negative claim itself ("nothing else shares that state") with instructions to sweep module-level mutables from the code. And the key insight that made the fix cheap: lock-key CONVERGENCE is the property to test, not lock presence — two surfaces "guarded" by locks with different keys over one folder are unguarded, so the test that matters is `lock_for_surface_A(X) is lock_for_surface_B(X)`. Both generalise well beyond mise.
