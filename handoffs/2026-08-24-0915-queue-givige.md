# Handoff — 2026-08-24 (queue-dispatched spike, mise-givige)

session_id: queue worker (loop dispatcher), no interactive session id
purpose: spike — settle whether Docs batchUpdate can CREATE a tab, verdict recorded on mise-wisuzu
format: fond-v1

## For the next Claude

### Done — mise-givige (CLOSED, verdict POSITIVE)

- **Docs batchUpdate CAN create tabs.** `addDocumentTab` is in the live discovery doc (docs:v1 revision **20260817** — it post-dates training data, so the live fetch was load-bearing) alongside `deleteTab` and `updateDocumentTabProperties`. Live probe as sameer.modha@itv.com on a scratch doc: add → 200 + minted `tabId`, `insertText` by `location.tabId` → 200, read-back showed the original tab's text intact with no redraft bleed. Scratch docs trashed. Evidence: `docs/research/2026-08-24-givige-tab-probe/` (README carries the full chain), commits `3ecd8e3` + `ab330a9`, both pushed to main.
- **Verdict + design constraints appended to mise-wisuzu's `--how`** (the outcome "mise can place content in a new Google Doc tab"). The build route is **two sequential batchUpdates** — add, read the minted id from `replies[0].addDocumentTab.tabProperties.tabId`, insert by `location.tabId` — still one `do()` call at mise's surface.
- **Fresh-context refuter: verdict STANDS, strengthened.** It independently refetched discovery (matched), confirmed `addDocumentTab` is GA on the public reference page (all Developer Preview markers there attach to the comments/suggestions family, which is also absent from public discovery — so discovery presence discriminates GA), and replicated the mechanism on a second scratch doc.
- **The refuter's falsifier found the one hazard worth remembering:** a single batch `[addDocumentTab{index:0}, insertText with no tabId]` returns **200 and silently writes into the ORIGINAL tab** — Location's "first tab" default does not resolve to the just-added tab even when it occupies index 0. Success code, wrong target: exactly the redraft-bleeds-into-live-draft failure wisuzu exists to prevent. On the brief and in the README; `probe_one_batch_fill.py` reproduces it.
- **Supplied tabId is refused categorically** (400 INVALID_ARGUMENT "Tab ID should not be specified in the properties when adding a tab", identical for `t.`-prefixed and bare formats — `probe_supplied_tabid.py`). So single-batch add-and-fill is impossible by design, not by format accident.
- **Sameer's pre-registered falsifier (`--badly`: "if we miss some clever and simple way it *could* happen across the bonkers API surface") adjudicated on the brief:** it aimed at a negative verdict; the verdict is positive on the primary surface, proven on two docs by two sessions, and the one candidate simpler route (single-batch) was probed and found silently-wrong. No adjacent-surface sweep needed for existence.

### Corrections folded (refuter catches — mechanism recorded, per house norm)

- First write-up said "byte-identical original tab"; the probe actually asserted containment + no-bleed (read-back carries Docs' document-final newline). Corrected in README and appended to the brief.
- "Cannot ride one batchUpdate" was inference when first written; the refuter's live probe upgraded it to a measured fact — and revealed the silent-wrong-tab hazard in the same run.
- The supplied-tabId evidence (step D of the probe ladder in the evidence README; both id formats, captured 400 body) initially lived only in the authoring transcript; now committed as `probe_supplied_tabid.py`.
- `probe_add_tab.py` originally exited 0 even if read-back checks failed; now returns 1 on any behavioural FAIL.

### Uncertain

- **Watch-grade:** no dated release note found for `addDocumentTab` (Docs release-notes URL 404s), and the live proof covers itv.com only. GA status on the public reference makes a domain lag unlikely; **the first mise-home (planetmodha) use is the free re-check** — no action before then.

### Risks

- None to code: this session's commits are docs-only (`docs/research/` is outside the vendoring boundary), no publish run, no source touched.
- Correction to the 0330 handoff's picture, caught by this handoff's cold reviewer: **the bapije publish already landed** — suite 1.74.0 shipped as `d6fe6b7` at 08:39 today, *before* this session's commits, and mise-bapije's step 5 is recorded done (re-verified against the installed artefact). Nothing outstanding there; do not re-attempt the publish, and the installed plugin does NOT still carry the serializer lock. (This handoff's first draft repeated the 0330 claim without checking `git log` — the borrowed-frame fault, mechanism recorded.)

### Opportunities

- **mise-wisuzu now has NO open action — the first move is filing the build action under it.** givige was its only action and is closed, so a cold reader waiting for a build item waits forever. The action should carry three shape questions: which op hosts the tab param (`create` targets a new doc, so probably `append`-adjacent or a new param on an existing op — mind the 2048-char do() description budget, CLAUDE.md "Three verbs is a budget"); the markdown question (mise's rich markdown rides the Drive whole-document import engine, `insertText` into a tab is plain text — native Docs requests per construct, or plain-text v1?); and the one-batch hazard as a regression test.
- **The Sheets sibling is already settled — no spike needed there.** mise-dahune closed 2026-08-17: `adapters/sheets.py::add_sheet` exists internally, adding a tab to an existing sheet is currently a deliberate refusal in `sheet_edit.py`, and exposing it is a dispatch decision that now rides the wisuzu family. (This handoff's first draft proposed re-running the spike pattern there — the reviewer caught it against the closed item's note.)

## For Claudes to come

Two shapes worth keeping. **A positive verdict can still get attacked on the falsifier's axis by inverting it**: the pre-registered `--badly` aimed at "concluded cannot from too narrow a surface"; with a positive verdict the residue became "missed a route simpler than the one we'd build", and probing that candidate found not a route but a hazard. **The worst failure class in a write API is 200-with-wrong-target**: the one-batch shortcut here doesn't error — it succeeds into the tab the feature exists to protect. When a probe returns success, read back *where* the effect landed, not just that it landed.
