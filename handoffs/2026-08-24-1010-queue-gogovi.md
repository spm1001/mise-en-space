# Handoff — 2026-08-24 (queue-dispatched build, mise-gogovi — item stays OPEN)

session_id: queue worker (loop dispatcher), same worker as the 0915 givige handoff
purpose: build the measurement-week showpiece as hosted HTML, staged for Sameer's reaction
format: fond-v1

## For the next Claude

### Done — the page is live, the bon is deliberately NOT closed

- **https://itv-mit.web.app/measurement-week/** — Sameer's showpiece draft (`~/notes/practices/mise/showpiece-draft.md`, v2 19 Aug, treated read-only) rendered as hosted HTML in ITV house style via mit-serve-html, with three inline-SVG charts. Byte-verified live twice (initial + post-essayeur redeploy; the byte-match is against mit-serve-html's cached render `sites/itv-mit/measurement-week/index.html` — serve.py wraps the source in house style at pull, so the live page is ~4× the size of the pre-wrap source here, and that is not drift). Page source: `docs/showpiece/measurement-week.html` in this repo (commits `732ccb5`, `f169c00`); serve ledger commits `a3732b3`, `1a8ad66` in ITV/mit-serve-html.
- **mise-gogovi stays open** — its done-criterion ("Sameer has shown it to someone and it landed without him having to explain it") needs him. The build evidence is appended to the bon's `--how`. Next move is his reaction pass. **To iterate the page:** edit `docs/showpiece/measurement-week.html` in THIS repo (hand-porting any wording changes from the draft md — there is no draft→HTML build script), then from `~/repos/itv/mit-serve-html`: `uv run --script serve.py pull measurement-week && uv run --script serve.py deploy`, and commit both repos. Editing the draft md alone changes nothing on the page.
- **His voice is preserved**: page prose is the draft verbatim except seven declared factual updates (list on the bon) and two declared vessel decisions — the standfirst's 636/918/60 enumeration moved into the stat-tile row, and the internal repo path dropped from finding 6 for the public surface. Both are flagged for his reaction, not hidden.

### The falsifier pass (--badly: "if it isn't updated to reflect where we got to since")

Two independent passes ran: a reconciliation sweep over findings docs + handoffs 19–24 Aug + Garni's repo, then a fresh-context essayeur attacking the built page. All six findings stand; nothing measured in the window reversed any of them. The five substantive fixes (all seven declared updates are on the bon — the other two are standing-URL datings and stripping the provenance comment from the public bytes):

1. **Garni carries ONE measured sentence, not two** — disclosure live at `mit-garni/app/agent_session.py:57-58`; show-working is agsp-sewena, open on mit-garni's bon board (as is agsp-cigene below).
2. **"Doubles in board-level documents" cut** — contradicted by the census's own stratum data (all 20 vision-only probes in the random stratum; purposive all-miss 0.5% vs random 5.7%). Replaced with "concentrated in chart-heavy conference and research decks".
3. **Token tax names its tokenisers** (+37.5% Gemini / +64% Claude) per the findings doc's own rule.
4. **1.67.0 shipped 18 August** — the essayeur caught both me and the sweep resolving the draft's "last night" to the 19th; the suite bump was 18 Aug 19:22. The one date the reconciliation touched was the one date it broke, until the refuter re-derived it from four git sources.
5. **"Garni's retrieval loop consumes exactly this" softened to "is being built to consume"** — agsp-cigene is open; only pixel delivery (view_file) is proven.

### Uncertain / notes for the iteration pass

- **Chart-aggregation coherence**: the page's token chart uses mean-across-slices (CSV +1.0%, md-min +2.9%); the deposit-format policy doc quotes +2%/+4% under a different aggregation. Both true; a cross-reader sees different numbers. The caption names the method — if it confuses the team, align the two on the next edit.
- **The census "610 text-reachable" convention** excludes 6 typography-drift probes that were technically text-reachable — inherited from census-findings.md, charge sits with the source doc.
- The vision-only stratum reconstruction (12 chart labels + 6 watermarks + 2 graphic labels) rests on arithmetic reconstruction, not a per-probe adjudication file — flagged by the essayeur as unauditable but consistent.

### Risks

- **The triptych PNGs are deliberately NOT on the page or the host.** `showpiece-triptychs-2026-08-18/` composites full-colour pages of real ITV Drive documents and competitor annual reports; mit-serve-html is public-with-noindex (anyone with the link). Hosting them is Sameer's go/no-go — he can preview them locally first at `~/notes/practices/mise/showpiece-triptychs-2026-08-18/index.html` so the call doesn't run on memory. If he wants them public-side, options: a separate artefact he approves page-by-page, or the walled route (mit-serve-html's `RECIPE.md` §Walled Route 2).
- **serve.py had a deploy-blocking bug, now fixed** (`3aa3ada` in ITV/mit-serve-html): `cmd_deploy` called `source_drift()` without `manifest_path` after the serve-gosecu refactor — every deploy TypeError'd before releasing. Anyone who deployed since that refactor would have hit it; we may have been the first deploy since.
- The draft file may be open in Sameer's Sublime — nothing here wrote to `~/notes`, and any page iteration should keep treating the draft as his copy (read, don't write, while a tab may hold it).

### Opportunities

- His reaction pass is the unblock: wording, the two vessel decisions, whether the triptychs join (his call), then "shown to someone" closes the bon.
- If the show-working sentence ships to Garni (agsp-sewena, one sentence + a before/after probe), the page's Garni bullet gets its second half back — one-line edit + redeploy.

## For Claudes to come

Refuters catch different failure classes at different ranges. The reconciliation sweep (given the sources) fixed four stale claims but re-derived one date wrong from a commit's local-date string; the essayeur (given the built page) caught that date against four independent git sources, plus an aspirational-present-tense claim ("consumes exactly this") that reads as shipped fact five days later. The generalisation: a claim in the present tense about a system's behaviour has a shelf life — when staging prose for a standing URL, hunt the present-tense claims and date or soften each one whose referent is still moving.
