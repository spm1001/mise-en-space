# Handoff — 2026-08-24 (queue-dispatched: mise-tanoti CLOSED, suite 1.77.0; cycle's last card)

session_id: queue worker (loop dispatcher) — one session, three cards today: wisuzu (1204 handoff), the dehebi evidence close (bon-note record), and this
purpose: the three consumer seams Garni's library rewrite filed this morning — crops opt-out, pdf_pages on core installs, last-modifier everywhere — built, measured, essayeur-hardened (essayeur = the loop's fresh-context falsifier subagent, briefed to refute rather than review), published
format: fond-v1

## For the next Claude

### Done — mise-tanoti (CLOSED): the three fetch seams, shipped as suite 1.77.0

- **`fetch(crops=False)`** skips PDF embedded-graphic crop extraction at the adapter (the cost is CPU, not disk), threaded server → remote → router → fetch_drive → fetch_pdf → adapter; anchors are written at deposit time from `result.crops`, so nothing dangles. Default True — the deposit contract for non-opting consumers is unchanged (pinned by tests/unit/test_pdf_crops.py::TestCropsOptOut and verified live by the essayeur). **Measured** (surface: tube, sameer identity, working-tree library, serial walk of the 10 real Garni corpus PDFs, thumbnails=False both arms): **325.3s → 31.9s (10.2×)**, deposits 122MB → 2.5MB, extracted text identical modulo anchor blocks and their surrounding blank lines. Attribution is per-file clean — deltas track crop counts (99.0→2.8s at 48 crops; crop-free files unmoved) and the essayeur measured the warm-cache confound at ~1s/file, so the 10.2× is genuinely crops. Garni's hydrate picks the flag up whenever its SHA pin moves (it pins `82a1f05`, pre-this); its 416s Cloud Run cold walk should land near the pre-crops ~40s.
- **`count_pdf_pages` shells `pdfinfo` directly.** It rode the pdf2image wrapper — an *extraction-extra* dep — so every core/library install returned None with the binary on PATH (Garni's image; the wujoga citation cue could never fire there). The essayeur proved the fix in a real core-only wheel venv; `tests/unit/test_pdf.py::TestCountPdfPages::test_counts_without_pdf2image` pins the independence, mutation-controlled. Bonus in the same direction: Gmail-attachment PDFs and off-PATH-poppler Macs now count too.
- **`last_modified_by` everywhere**: `lastModifyingUser` in both Drive fields masks; on search rows (explicit null when absent) and in every Drive deposit manifest via `add_file_provenance` — which moved to `tools/fetch/common.py` beside its sibling deposit helpers rather than raising the frozen file's baseline (the ratchet-as-design-force pattern; `tools/fetch/drive.py` net SHRANK below its baseline, while `adapters/drive.py` — a different file — took a +5 argued raise for the mask lines that structurally live only there). Live on the corpus: `owners: []`, `last_modified_by: 'Sameer Modha'` — on a Shared Drive the last-modifier is the only author signal, and Garni's MANIFEST "by <who>" line now renders from mise data alone. Zero extra API calls.
- SHAs: `fccff88` (the three fixes) → `5b2eb90` (essayeur fold) → published as **suite 1.77.0** (assemble green, installed artefact verified: crops threading, both masks, pdfinfo subprocess). Board residue filed: **mise-jefaki** (hyphenated-name Drive search blindness — judged search-instrument scope, filed separately; the originating orbit note lives in `bon show mise-tanoti`'s brief.how).

### The essayeur round — nothing corrupting; the catch was in MY evidence chain

- The measurement script's content hash stripped anchor *lines* but not the blank lines anchor insertion adds — so my own committed artefacts showed differing shas on every crop-bearing file while the commit prose said "byte-identical". Substance true (essayeur adjudicated live: non-whitespace streams byte-equal, 133,750 chars both arms); instrument said otherwise; nobody compared the shas before the claim shipped. Folded in `5b2eb90`: blank-normalised hash, identity captured after the walk (the first cut committed `identity: null` while claiming the sameer surface), both arm JSONs committed beside the script at `docs/research/2026-08-24-tanoti-measure/`, plus three pdfinfo parity edges (stale docstring, `Pages: 0` falsy guard, tmp-leak on failed write). NB the committed JSONs are the FIRST cut's output kept deliberately warts-and-all: their cross-arm `content_sha` mismatches on the 7 crop-bearing files ARE the instrument fault this section describes (and `identity: null` is the lazy-resolution wart) — a fresh reader grepping the shas will see 7 mismatches, and that is the exhibit, not a live discrepancy; the warm-cache ~1s/file re-run and the 133,750-char adjudication are the essayeur's live measurements, recorded here and in the bon note only.
- Pre-existing gap it confirmed and we left named-not-hidden: Gmail attachment PDFs never extracted crops on any path, before or after this build.

### Uncertain / watch

- `do()` description headroom is **102 against the 100 floor** — the next op or param spender meets the budget test immediately; rebalance-by-compression (the wisuzu precedent) is the move.
- Garni-side follow-through (their lane, not ours): bump the mise pin past `5b2eb90` and pass `crops=False` in hydrate_drive; re-run `uv pip compile` per their own handoff's lesson.

## For Claudes to come

**The facade's typed returns ate four probes in one session.** `FetchResult`, `SearchResult`, `FolderFile` are dataclasses (`.get()` AttributeErrors), and search's `to_dict()` is PREVIEW-shaped — rows live in the deposit file, not under a `results` key, and a `.get("results") or []` reads a healthy 11-hit search as an empty corpus. The truncation cue even says "read the deposit before concluding anything is absent" — my probe hit exactly the trap the cue names, twice. Probe the facade with attributes and the deposit, never dict-guessing.

**A claim your own instrument contradicts is worse than an unmeasured one.** "Byte-identical" shipped in a commit message while the sha column of the very artefact it cited showed seven mismatches — the comparison that would have caught it (grep the shas across arms) costs ten seconds and simply wasn't run before the write. When an artefact carries a checkable column, check it against the sentence you're about to write; the essayeur's framing is the durable one: *substantively true, but the instrument never showed it and visibly shows the opposite* — that's still a false record.

**When the ratchet blocks a frozen file, the sibling question usually has a real answer.** `add_file_provenance` belonged in `common.py` beside `deposit_pdf_crops` and friends all along — the ratchet forced the better home (drive.py net shrank), while the five mask lines that structurally could not move got a small argued raise. Two mechanisms, each used where it fits, neither defaulted to.
