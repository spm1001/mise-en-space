# Handoff — 2026-08-24 (queue-dispatched: mise-ligiru CLOSED as DECLINED, no code shipped)

session_id: queue worker (loop dispatcher), the ligiru demand-check card
purpose: rich markdown into a Doc tab (fidelity v2) — demand check ran first per Sameer's binding steer, came back empty on every instrument, card declined; the full evidence lives in the bon done note
format: fond-v1

## For the next Claude

### Done — mise-ligiru (CLOSED, DECLINED): the demand check says nobody needs rich tab content yet

- **No code changed.** The card's done criterion had two outcomes — build fidelity v2, or "the demand check says nobody needs it and this closes as declined" — and the evidence landed squarely on the second. The demand check ran first because Sameer made it binding (2026-08-24 falsifier conversation, quoted in the card's how-field): he challenged the whole md→Doc premise unprompted — "why are we converting PDFs to Docs anyway?" — and the paste-flow cousin got "only monsters do that". That steer bears on the whole fidelity programme, not just this card. The full instrument-by-instrument evidence (call-log counts, cue-receipt sweep, the 2026-08-05 original ask, the ship-day flagship session, the multi-tab census, the cross-user field-report null) is in `bon show mise-ligiru`'s done note; don't re-derive it, read it there.
- **The shape of the finding**: tab placement was really demanded (2026-08-05, prose alternatives beside a shared draft) and v1 serves that; rich rendering in tabs has never been asked for, never been complained about, and the one real redraft session on ship day (Sameer's 2027 budget doc) used five whole-doc rich overwrites on a single-tab doc instead — the existing rich route served. Multi-tab Docs in real use are Gemini meeting-notes artefacts, consumption not redraft targets (9 of the 55 tab-measurable deposits; 94 older manifests predate the `structure` field and cannot report tab count — quote 9-of-55, never a percentage over the 149).
- **Essayeur verdict: CONFIRMED** (essayeur = the loop's fresh-context falsifier subagent, briefed to refute the close rather than review it), after attacks on the logger's param whitelist (tab IS logged), the commis transcript lane (a symlink to the same corpus), unswept `~/notes/raw/` subdirs (false positives only — one was "separate tab**le**" clipped by the regex), and a genuine wobble: 12 of the 18 tab-bearing log calls say `create tab='Redraft'` inside the flagship session's time window, which briefly read as the real session hammering the feature. It was the dev loop — synthetic file_ids (`doc1`, `/tmp/nowhere.md`), refusal text mutating mid-window as `73b1da4` landed, and 'Redraft' is the SKILL.md example value. Three essayeur folds (honest denominator, the Redraft correction, a named reopen trigger) are all in the done note.

### The reopen trigger — the decline is falsifiable, not final

A future session reopens this (new card, cite mise-ligiru as progenitor) on either signal:

- **Authoritative instrument: the call log.** A non-dev `tab=` call in `~/.local/share/mise/calls.jsonl` — real file_ids, real content lengths, from a session that isn't building mise itself. This is the instrument that cannot be poisoned by reading about the feature.
- **Corroboration only: the transcript sweep** — `rg -li -F 'not rendered in tabs' ~/.claude/projects -g '*.jsonl'` — and it carries THREE noise classes, not two: `isMeta` skill-injections (the string ships in `skills/mise/SKILL.md`), build-session echoes, and — the one this handoff itself creates — read-echoes, because the string is quoted here and in the done note, so any session that Reads this file or runs `bon show mise-ligiru` mints a tool-result containing it (measured live: 8 files at close, 10 within two hours as the essayeur and cold reader ran). A real receipt is the cue inside a **mise `do()` tool result**, nothing else.
- Any field report asking for rich Doc-tab content (field reports are the only cross-user instrument — MIT marketplace and mise-home users are otherwise unobservable from tube).

Both tripwires ship in 1.76.0+ and sit exactly on the demand path: the always-on plain-text cue on every `append tab=`, and `overwrite`'s multi-tab refusal teaching `append tab=`. If reopened, the card's brief still carries the plan — the design sketch in `why` (scratch-doc replay: import md to a scratch doc, `documents.get` the rich structure, replay per-construct batchUpdate into the target tab, trash the scratch), the sizing and the rubucu lesson in `how` (a compiler first cut WILL carry silent-wrong; the essayeur pass is part of the build, not after it).

### Uncertain / watch

- **The latent pocket, named so nobody rediscovers it as a shock**: since mise-wisuzu (the plain-text v1, shipped 2026-08-24), a multi-tab Doc has NO rich write route at all (overwrite refuses, surgical ops are plain) — and every `tab=` use mints another multi-tab doc. Today that's a design-debt forecast, not demand: real multi-tab docs are Gemini notes artefacts nobody redrafts into. The tripwires above are pointed at exactly this pocket. Watch-grade, holder: the reopen trigger.
- The cue-receipt leg had ~11 hours of runway at close (cue shipped 12:04, checked ~23:00 same day). It was treated as corroboration only — the load-bearing legs (the prose-shaped original ask, the flagship session's revealed preference, seven weeks of call log, the complaint sweep) are not time-thin. A future reader re-running the receipt sweep gets a better-powered version of the same check free.

### Risks

- None open. No code, no publish, no scratch docs created (the declined path never reached the spike — zero cleanup owed). Board write verified read-back (note_len 3114). Working tree was clean at `084a70f` before this handoff; this file commits with the cold-read folds already applied.

### Opportunities

- **mise-vuloju** (open watch card) holds the `# Tab 1` + hrule fetch-furniture clause — unrelated to demand but adjacent tissue if anyone works the tab surface again.
- The manifest census trick (`find ~/notes ~/repos ~/scratch -maxdepth 5 -path '*/.mise/doc--*/manifest.json'` — run on tube, 2026-08-24 — then read `structure.tabs` from each) is a cheap population instrument for any "how common is X in real Doc use" question — with the denominator caveat: only manifests newer than the mise-dogape enrichment (~2026-07-27) carry `structure` at all.

## For Claudes to come

**A demand check is a null result, and a null is a claim about the paths searched — so the close names its instruments, its known-positive control, and its reopen trigger.** The control here: the cue-string sweep had to find today's build subagents (it did — six files), or the sweep itself was broken. The reopen trigger is what keeps a DECLINED close honest: without one, "nobody needs it" hardens into "nobody ever asked", which is a different claim nobody measured. And one instrument fault the essayeur caught is worth generalising: **a denominator that silently includes unmeasurable members lies while being arithmetically true** — 9-of-149 (6%) read as rarer than 9-of-55-measurable (16%); when a schema field arrived mid-corpus, the census splits into measurable and not, and only the measurable half is a denominator.
