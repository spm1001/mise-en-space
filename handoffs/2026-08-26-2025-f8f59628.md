# Handoff — 2026-08-26

session_id: f8f59628-684b-4edb-8f81-4ae53cc2ab7d
purpose: mise-rijeco acceptance test ④ — the 1:1 programme booked live through mise, Sameer present for one approving pass; plus a side-quest booking and the morning /open synthesis
format: fond-v1

## For the next Claude

### Done

- **Closed mise-rijeco** — test ④ passed: 8 fortnightly 1:1 series + 1 bridge booked through mise in one session (all 45 min, Sameer organising, each person's standing "First | Sameer" doc attached, Meet on remote, `1to1=<name>` extendedProperties key on every event). Full evidence, grid deviations and probed facts in the item's done note — read it there, don't re-derive.
- Strips executed: Stef + Rupert old series declined, Ellie 18 Sep + Judi 17 Sep instances declined, Ella's old series RRULE-truncated (read-back: zero surviving instances), Lauren's 28 Aug instance moved to 09:00–09:30 on Sameer's call. Three Review-block series bumped 08:30→08:00 (verified live by their absence from later clash previews).
- Rooms verified on the rooms' own freebusy: 3.D.M3 holds 27 Aug + 3 Sep 09:00–09:45, 3.A.M4 holds 8 Sep 10:45–11:30.
- Side quest: **MeasureCon Content Brainstorm** booked Fri 18 Sep 11:30–12:15 (Sameer + Lauren + Marina Bishop + Sophie Boyes) — the only common 45-min slot in three weeks. "Marina Warner" does not exist at ITV; Sameer confirmed he meant Marina Bishop.
- Filed from live fire: **mise-lemawe** (freebusy reads naive datetimes as UTC while create_event resolves diary-tz), **mise-dodapa** (sole-guest-declined event renders like a live meeting — Sameer caught it from memory), **mise-cacica** (wind up Ellie/Judi old series after their kept instances pass).
- Annotated **mise-redewa** with the live grid, the reconciler's keyed query, and the first-reconciliation watch-list; added its dispatch line to the Toolmaking queue under the tell-after norm.
- Todoist: the 1:1 dispatch task + queue line and the two stale mise lines (huwubi/bapije done; tesuto/taciku parked) completed with notes — **under Sameer's explicit instruction**, not the tap norm.
- Morning /open: ligiru demand-check lessons synthesized into understanding.md (`aa6ad8c`); Dependabot auto-merged state verified locally (2773 passed, 46s) — the no-CI-on-merge control run, benign.

### Reflection

The plan-then-approve shape worked exactly as the brief hoped: ~20 mining calls produced a grid that deviated from the 19 Aug sketch in four diary-forced ways, Sameer approved once with four one-word sub-decisions, and the whole batch (3 bumps, 6 strips, 9 creates) ran preview→confirm without a single 400. The preview layer earned its keep twice — as clash-checker and as *verification instrument*: earlier writes were confirmed by their absence from later clash lists. Sameer's "are you sure?"-class catch (Kate had declined) was data I already held and never read — the fact was in `attendees[].status`, below eye level. That's now mise-dodapa, but the session lesson is older: read the response fields of the thing you're about to propose a decision about.

### Uncertain

- The "Kate / Sameer" Mon 08:30 event carries an instance-suffixed id (`…_20260914T073000Z`), so it may be a decayed fortnightly series rather than a one-off — if another instance appears ~28 Sep, same treatment (she declined; dead furniture).
- All nine new bookings sat at `needsAction` as of ~20:30 — no acceptances yet. Expected within a day; the reconciler (mise-redewa) owns watching it.

### Risks

- **Four UI-deletes rest with Sameer** (mise cannot delete events): Lauren's orphan instances Fri 11 + 25 Sep 15:00, Ella's new-series instance Thu 10 Sep 09:00 (minted into his conference week — the 8 Sep bridge replaces it), and Kate's declined Mon 14 Sep 08:30. Until deleted they sit in his diary as busy/live-looking furniture and will confuse any freebusy over those windows.
- Lauren's 15 Sep anchor has **no room** (all four candidates busy) and sits on her 10:30 Be Studio hold pending her nudge — if she pushes back, the anchor moves and the reconciler should catch it.
- The 18 Sep four-way was mined by freebusy, which is blind to unaccepted invites (mise-sicena) — a pending invite on any of the four could still surface a clash.

### Opportunities

- **mise-redewa is the successor** — the programme is live, so the intent file + first hand reconciliation is now real work with real data; its `--how` carries the grid, the keyed query and the watch-list, and its dispatch line is in the queue.
- **mise-cacica fires after 3–4 Sep** — two one-call series declines once the kept instances pass.
- **mise-lemawe and mise-dodapa are small, sharp fixes** with fresh reproductions in this handoff's session; lemawe is the more urgent (it silently shifts every naive freebusy window an hour).

## For Claudes to come

**Google Calendar recurring series decay into orphan exceptions, and recurrence surgery cannot reach them.** Lauren's old 1:1 master carried `UNTIL=20260813` while three instances (28 Aug, 11 Sep, 25 Sep) lived on as exceptions — a past truncation left them standing, and a new RRULE patch on the master does nothing to them. So "strip the old series" decomposes by mechanism, not by wish: rule-generated instances die with an UNTIL truncation; exception instances need per-instance action (decline if someone else organises, UI-delete if you do — mise has no delete op by design). The diary tell: an instance whose master's RRULE could not have generated it. Two instrument facts travel with this: **calendar search never renders resource attendees**, so the read-back for a room booking is the room's own freebusy, not the event's attendee list; and **freebusy reads naive datetimes as UTC while create_event resolves them to the diary timezone** (mise-lemawe) — until fixed, explicit `+01:00` offsets on every calendar call, and treat any freebusy window echo that disagrees with what you asked as the instrument, not the diary.
