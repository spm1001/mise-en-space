# Handoff — 2026-08-31

session_id: cd521620-3706-4354-a22f-4e2c7f3081fc
purpose: The DPP anchored-comments day — same-day probe of Google's preview announcement, five build/measure items filed, preview-tracking designed out of inbox luck (watcher filed, not yet built; manual weekly stands in)
items: mise-picihi, mise-mikawi
format: fond-v1

## For the next Claude

### Done
- Fetched the day's Developer Preview announcement (Slides API anchored comments, joining Docs comments+suggestions and Sheets comments) from Sameer's pasted Gmail URL, filed mise-picihi, and ran the whole probe same evening — closed it (bon note carries the summary).
- Proved enrollment live through mise's existing client+token: read (commentsViewMode), create (anchored insertComment), reply/resolve/reopen all 200 on Docs, Slides and Sheets. Evidence: 42 numbered artefacts + NOTES.md in `docs/research/2026-08-31-anchored-comments-probe/` — that NOTES file is the map; read it before re-deriving anything about this surface.
- Confirmed UI-authored comments carry anchors on all three surfaces (Docs+Sheets via live colleague-commented files, redacted evidence; Slides via Sameer's hand-made deck comment — shape-level anchor with character range).
- Settled the mise-mikawi mechanism with Sameer's screenshot (evidence 42): Drive-plane unanchored comments render nowhere in the document — Comments-panel-only, under "Original content deleted". Annotated the item three times as evidence landed (candidate fix, mechanism detail, verdict). **The item stays OPEN** — its residual (unanchored do(comment) should cue that its output is panel-only) is now homed on mise-jupuja rather than laneless.
- Probed suggest mode: `writeControl.writeMode=SUGGEST` turns Docs batchUpdates into tracked-changes suggestions; acceptSuggestion works; same-author suggestions coalesce into one thread (accepting one accepted both — measured).
- Filed the follow-on lattice: mise-dukacu (comments.md slide/cell locators), mise-jupuja (anchored do(comment) + assignee), mise-hupago (suggest= lane), mise-natila (measure Google's official preview MCP servers against mise, under mise-zidipo), iw-pigeso (DPP watcher, infra board).
- Wrote Sameer's tracking into Todoist: weekly Monday check in & At Timed (task 6hPwWfMJHfJP7mjR, retires when iw-pigeso ships — supersession hook on both sides), and five dispatch lines into Up Next at his ask.

### Reflection
- The whole arc — announcement email to closed probe with five filed successors — ran in one evening because enrollment already existed and the token already carried every needed scope. The expensive part of preview adoption here is *noticing*, which is exactly what iw-pigeso automates.
- Sameer's soft observations twice beat the rig: "went straight to resolved" reframed the mikawi mechanism, and "adding it for agents" predicted the catalogue (official Google MCP servers) before I read it.
- Two-script archaeology (probe.py then followup.py fixing the commentId extraction path) kept evidence numbering clean; the redaction discipline for live-file evidence (public repo!) is in reread2.py and worth copying.

### Uncertain
- **Where enrollment actually rides.** Our combo (sameer.modha@itv.com + the mit-workspace-mcp-server client) works; whether the user's DPP membership or the registered Cloud project is the operative leg was never isolated. Consequence if it's the email: other @itv.com mise users get refusals on preview lanes. The build items already design for this (fail open with a cue), but the one-variable test (another teammate's token, same client) hasn't run. Verdict: watch — nothing breaks meanwhile; the test needs a teammate's token, which is Sameer's to recruit.
- Drive-plane anchor legibility is confirmed only for API-created comments; UI-authored comments were confirmed via the preview reads. mise-dukacu's plane choice depends on closing that gap (its brief says so).
- Docs body-structure anchor positions (where kix anchor ids surface inside document content, beyond plainTextQuote) — unprobed; likely unneeded since Docs comments.md already locates via content correlation.

### Risks
- Everything probed is **Developer Preview**: 3–6 month typical residence, no SLA, surface can change before GA. Term iv bars shipping pre-GA features to customers — mise is publicly distributed, so every preview lane must degrade gracefully for non-enrolled users; that's written into dukacu/jupuja/hupago as a compliance requirement, not politeness.
- Suggestion granularity is Google's: adjacent same-author suggestions coalesce, and accept operates per-thread. Any future suggest= surface that implies per-edit accept is writing a cheque the API won't honour.
- The mise skill's line "The API can't create suggestions" (skills/mise/SKILL.md) is now false *for enrolled callers only* — deliberately left unedited, since it stays true for shipped users until hupago lands; hupago's brief carries the docs rider. CLAUDE.md's suggestions row is read-side and still TRUE — when hupago ships it needs *extending*, not correcting. (The cold reader caught this summary hardening "stale" into "false"; don't go hunting a false sentence in CLAUDE.md.)
- The probe scratch folder (doc/deck/sheet) is still live in Sameer's Drive — deliberately, SUGGESTED-C is viewable there; NOTES.md links it. Trashing it is Sameer's, no deadline — nothing depends on it.

### Opportunities
- mise-dukacu, mise-jupuja, mise-hupago are queued in Sameer's Up Next and each brief is execution-ready with tonight's evidence cited — dukacu is the natural first pull (read-side only, no write hazards).
- mise-natila is the perishable one: Google's official Workspace MCP servers (Gmail/Drive/Docs/Sheets/Slides/Chat/People/Calendar + Universal Search) are in preview NOW and typically stay 3–6 months — measuring them against mise gets easier never, only less possible.
- iw-pigeso (infra board, open — designed, NOT yet built) is the watcher that would automate preview-tracking; its brief carries the full design (devknowledge-API diff of the DPP page + release-notes Atom feed → board items on change) and the hook to retire Sameer's manual Monday check on its first verified run.
- mise-redewa (1:1 intent file + first reconciliation) remains the standing successor from the previous handoff — untouched tonight, still live, still in the backlog lane.
- mise-cacica is now marked waiting ("kept 1:1 instances passing, unblocks after 3–4 Sep") so the date lives on the board, not just in prose — after it passes, wind up the old Ellie/Judi series.

## For Claudes to come

**Google is building the Workspace comments/suggestions API surface for agent readers, and the tells are structural.** The 2026-08-31 DPP release completed anchored-comment read+create across Docs/Sheets/Slides (one model: `commentsViewMode` on the read, `insertComment`/`addCommentReply` via batchUpdate on the write), and every thread carries `plainTextQuote` — the anchored text repeated as a string, which no human UI needs and every API reader does. The same programme ships official Google MCP servers for Gmail/Drive/Docs/Sheets/Slides/Chat/People/Calendar plus a "Universal Search MCP server" — first-party agent tooling, and mise's first first-party rival. Practical facts that outlive the session: mise's existing OAuth client+token reach all preview endpoints with no extra ceremony; Docs' comments read demands `suggestionsViewMode` be explicit AND `includeTabsContent=true` (bare `commentsViewMode` 400s) while Sheets/Slides need nothing extra; Drive-plane document-level comments are invisible in the document UI (panel-only, "Original content deleted") which is the long-standing mise-mikawi mystery solved; suggestion writes coalesce by author-adjacency and accept per-thread; service accounts cannot enroll in the DPP (domain-wide delegation is Google's stated route to agent identity); and pre-GA features must not ship to customers, so preview adoption in a distributed tool means designed degradation, disclosed in cues. The full probe map with 42 evidence artefacts: `docs/research/2026-08-31-anchored-comments-probe/NOTES.md`.
