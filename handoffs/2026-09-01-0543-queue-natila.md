# Handoff — 2026-09-01 (queue-dispatched: mise-natila CLOSED)

session_id: queue worker (loop dispatcher, early morning batch)
purpose: Google's DPP Workspace MCP servers measured against mise on all four card dimensions; essayeur refuted the first draft's delivery headline and the fold made the bench stronger
format: fond-v1

## For the next Claude

### Done — mise-natila (CLOSED, bench leg)

The card's four dimensions are all **measured, not asserted**, in a dated note at `~/notes/practices/mise/google-dpp-recheck-2026-09-01.md` with raw runs — including a `.request.json` beside every re-run response — in `~/notes/practices/mise/natila-2026-09-01/`. The public repo gets only a method pointer (`docs/research/2026-09-01-natila-google-mcp/README.md`) because the repo is PUBLIC and DPP term iv scopes pre-GA findings to the programme.

Headlines: Tier 0 fresh — mise 2,802 / Google trio 39,644 (14.2×) / taylorwilsdon v1.25.2 core trio 8,738 (3.1×); the margin narrowed from 15.9× because *mise grew* (suggest/anchor features + SDK vintage). Fact recall — mise 14/14 text probes, Google 10/11 reachable, and both Google gaps are wrong-answer traps (comment layer silently dropped with the permission plane controlled; attachments disclosed but no fetch route in all 55 schemas). Shared Drive reach: **parity** — a suspected mise advantage, scratched by measurement. Search honesty: Google fixed pageSize/nextPageToken/metadata on the per-product servers in 31 days; `search_corpus` still ignores pageSize with no signal (asked 5, got 30, ~38K tokens — replicated on two queries).

### The essayeur earned its dinner

Briefed as the card's falsifier (a rival maintainer's Claude hunting unfair configuration), it **refuted the draft headline**: "Slides delivery is 170×, only route 98K tokens" died to one masked call — `read_presentation`'s `fields` mask, which the schema itself instructs agents to use, delivers everything at 680 tokens, cheaper than mise's 799. The fold replaced the dead claim with a better finding: the delivery cost is a **three-point curve** (680 expert / 2,368 documented-example / 98,069 bare), and the middle point — **the schema's own worked example** — returns a plausible deck read with the speaker notes silently gone. Fold count: read 8 / folded 6 (with fresh re-runs, not wording patches) / resolved-by-revert 1 / declined-with-reason 1 (the `search_corpus`-over-attachment probe needs a mailbox surrogate that doesn't exist without inserting mail; the settling probe is named in the note).

It also caught: mise's own delivery figures under-counted (cues responses weren't archived — they are now, and every figure recomputes from an archived artefact), recall arithmetic (15 vs 14+1), trio tool-count slips, and indent-inflated anatomy numbers. Every correction is in the note with the losing reading kept inline.

### State changes a future session should know

- **Corpus link grant: created and REVERTED.** The bench corpus folder was anyone-with-link (reader, then commenter) for the run; deleted at session end, revert verified by anonymous content probe per the traps.md Google Workspace row. Reproduction: one re-grant via `natila-2026-09-01/tools/share_corpus.py`.
- **Nothing was inserted into any mailbox**; F04's Gmail leg was answered as a capability question on the ITV mailbox's existing content.
- **The August spike doc (`docs/2026-08-01-google-mcp-spike.md`) is fully public in this PUBLIC repo and carries detailed pre-GA DPP findings.** Pre-existing state, not touched. Verdict: **watch** — whether it stays public under term iv is Sameer's call, not a worker's.
- **DPP enrollment account-vs-project leg: still untested, now sharpened.** A planetmodha-token call fails first at `SERVICE_DISABLED` on its own client project (903554315162), so it cannot discriminate the enrollment key. The discriminating experiment is: enable `drivemcp` on that project, retry, read which error changes. One console action + one call — **for Sameer/dispatcher to sanction**, filed here rather than run (dispatcher's standing watch).
- Preview servers drift fast: two of our own August invocations now error (`search_files` free-text; `messageFormat: FULL`). Any future run should re-harvest first and expect renamed enums.

### Open threads

- The note names one unprobed question: whether Gmail's search index surfaces attachment-body text through `search_corpus` snippets. Settling probe named in §5 of the note.
- mise-nihebo (ITV twin corpus) narrows in scope: SD *reach* is parity, so its remaining value is SD permissions edges and tenant posture.
- Gmail relevance ranking still rests on the 1 Aug measurement; a re-run needs the mise-side composed search compared query-by-query — half a session if wanted for mise-gogovi.

Board: verdict paragraph appended to mise-zidipo `--how` (dated NATILA RUN 2026-09-01); card closed with fold count in `--note`.
