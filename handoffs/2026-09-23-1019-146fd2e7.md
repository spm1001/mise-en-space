---
session_id: 146fd2e7-884c-46c9-8ca1-86d160aea846
purpose: Whole-board read and a silent-wrong-answers campaign - 32 cards closed, drafts made safe, live integration suite repaired, 40 deps refreshed, every fix live-checked and essayeur-refereed
author: claude/opus-5.5
items: mise-tiroti, mise-kuvuwe, mise-suwopu, mise-josebe, mise-cacogi, mise-zofoja, mise-duralu, mise-lemawe, mise-bonezo, mise-dodapa, mise-kubolo, mise-mudupa, mise-newidu, mise-womuse, mise-cesico, mise-zefele, mise-decuvu, mise-lubuce, mise-sajeso, mise-pirusu, mise-sokaji, mise-sasoti, mise-bewono, mise-mikawi, mise-gogovi, mise-pokahe
format: fond-v2
---

# Handoff — 2026-09-23

## For the next Claude

### Done
- **Read the whole board (117 open) and scrubbed it** — ten spent or duplicate cards closed with notes (duplicates folded into one survivor each; `mise-mikawi` among them, its SKILL.md residue fixed in eb14684), waiting states corrected, `mise-jafimu` filed from the atelier's deck field report. The board now shows 12 open outcomes plus standalones, about 90 open items in all.
- **Silent-wrong-answers campaign, every fix live-checked on Sameer's ITV account (disposable files, cleaned up):** view-only Docs now fetch (`mise-tiroti`/`mise-kuvuwe`, the top real error in the call log); multi-tab sheet create names tabs right and resolves forward references (`mise-suwopu`, logic moved to `tools/sheet_create.py`); binary overwrite in place (`mise-josebe`, plus a create bug it exposed: MIME from an extensionless title); `?` in tab names (`mise-cacogi`); `tabs=` miss refuses (`mise-zofoja`); minute-precision calendar times and freebusy's timezone (`mise-duralu`, `mise-lemawe`); calendar search description/rooms/all-declined (`mise-bonezo`, `mise-dodapa`); notification mail keeps its links (`mise-kubolo` — the root cause was a Beautiful Soup bug fixed upstream in 4.15.0; now a dependency floor, not a workaround).
- **Drafts made safe:** updates keep attachments (`mise-mudupa`); reply drafts skip trashed tail messages, accept `to=`, warn when the originator is missing, and state their addressing (`mise-newidu`, `mise-womuse`, `mise-cesico`); an update warns when someone else wrote the draft since mise did (`mise-zefele`, `mise-decuvu`). **Sameer verified in Gmail that reply drafts render inside the conversation**, closing `mise-womuse` and `mise-lubuce`.
- **Pasted images stop vanishing** (`mise-sajeso`): judged by size not the generic `image.png` name; every attachment in a thread gets one ref used for the deposit name, the hidden list (`cues.hidden_attachments[].fetch_as`) and `attachment=`; repeats across replies are deduped.
- **Platform:** live integration suite repaired, 107/107 (`mise-pirusu` — 13 reds were all stale tests, one had been passing vacuously, plus a real bug: `activity_id` was always empty); lock refreshed in three gated slices, 40 packages incl. MCP SDK 2.0→2.2 and markitdown 0.1.8 (`mise-sokaji`); CI now runs on Dependabot-merged trees in mise, bon and trousse (`mise-sasoti`, open until a real merge shows the dispatched run); the unit suite no longer writes the real call log (`mise-bewono`).
- **Filed:** `mise-pokahe` (guidance weight) with `mise-vacima` (skill diet) — the skill is ~18-19k tokens against a 2,802-token tool surface; `mise-wowagu` (autolinked notification CTAs still stripped, now disclosed); `carte-docide` on the ~/.claude board (Fable essayeurs die of inherited-corpus context).
- **Showpiece page** now carries a "Needs updating" banner (live at itv-mit.web.app/measurement-week/); Sameer did not recognise the page when asked, so `mise-gogovi`'s show-someone criterion is unmet. Verdict: watch — no build work; it waits for Sameer to say whether the page is still wanted (his to raise, not a thing to chase).
- **Sameer's dispatch queue** (Todoist project '& Toolmaking', read with `accomplis tasks --project "& Toolmaking"`): reworded the competitive-benchmark line (`mise-zidipo`: all 60 pilot spawns have now run) and the 1:1-reconciler line (`mise-redewa`: its first report awaits Sameer's read); added lines for `mise-jikiwi` and `mise-jafimu`.

### Reflection
The essayeur pass earned its place every round. Three rounds, three sets of real defects, including one of mine that went to Sameer as a claim: I reported "both budget slides now come through" after checking filenames, and the two files were byte-identical — one slide carried by two messages, the real second slide still unreachable. The fix (thread-wide refs, dedupe) came from the refutation, not from my own check. The Fable essayeurs died of context twice before reading much; an Opus 1M retry worked. Sameer's soft "are you sure about Beautiful Soup?" turned a planned upstream bug report into a one-line dependency floor — the bug was real and already fixed in June.

### Uncertain
- Whether multi-tab sheet create's re-entry of tab 1 (USER_ENTERED, only when tab 1 holds formulas) ever disagrees with Drive's CSV type detection on dates or leading zeros — unmeasured, noted on `mise-suwopu`.
- Why Dependabot's weekly uv group never bumped mcp or markitdown — moot while the lock is refreshed monthly, recorded on `mise-sokaji`.

### Risks
- **Today's work is already live, not waiting for tomorrow.** Batterie reassembled seven times today (other sessions' publishes plus one dispatch at close); mise 1.86.5 (@ 931c2d4, which includes every commit above) was assembled at 09:25Z. For about 20 minutes before that, 1.86.4 carried the essayeur-refuted first version of the image refs (434e8e0) and reply_draft without 0e2b1bb; that window is closed. Library consumers (glaneur, Garni) that parse deposit filenames (`image-2.png`), rely on silent `tabs=` misses, or read freebusy's naive times as UTC are the likeliest to notice. CLAUDE.md's 'waits a day' line was corrected at close.
- **Docs lag the code in two places (act, next session in this repo).** CLAUDE.md's Key behaviors list has none of today's behaviours (reply_draft `to=`, draft attachment carry-over, hidden attachments and refs, calendar search fields). SKILL.md has the draft and calendar changes but not hidden attachments / `fetch_as` refs (`mise-sajeso`). One bullet each would close it; weigh against `mise-pokahe`'s guidance-weight goal.
- `attachment=` with several same-named parts now resolves by ref (`image-2.png`); a caller holding only the original filename gets the first. Benign — the hidden list and deposit names carry the refs.
- The draft concurrent-edit warning is process-local: after a server restart there is nothing to compare, and only the always-on `compose_window_note` remains.

### Opportunities
- **Live-check installed mise 1.86.5 through the real plugin tools.** It is already assembled (09:25Z, @ 931c2d4); run `/batterie:update` or start a fresh session, confirm the version with `git -C ~/repos/spm1001/batterie show origin/main:plugins/mise/.claude-plugin/plugin.json`, then a few calls through `mcp__plugin_mise_mise__*`: a view-only Doc fetch, a draft update carrying an attachment, and the budget thread 19fa84f9998d1c2a (expect image.png deposited and image-2.png listed as hidden).
- **Close `mise-sasoti` on the next Dependabot merge (they land Mondays ~12:15Z).** Read `gh run list --workflow ci.yml` in mise-en-space, bon and trousse for a `workflow_dispatch` run on the Dependabot merge commit — not today's hand-triggered test dispatches (08:12Z and 08:56Z), which prove only that dispatch works.
- **Next build cards, as ranked with Sameer:** `mise-jikiwi` (tables into existing Docs, asked for four times by the atelier), then `mise-jafimu` (pptx to Slides on create plus every-slide render; fold `mise-vogoro` arm 1), then `mise-wowagu`.
- **Hold `mise-vacima` (skill diet) until `~/notes/practices/mise/tier1-pilot-2026-09/results.md` exists.** Its ablation reuses the Tier 1 pilot harness (mise vs Google on read-only intents, `mise-lewube`); writing results.md is that card's step 4, and Tier 2 is Sameer's call after it.

## For Claudes to come

**A live check must compare the artefact, not its name.** This session reported a fix verified because two files named `image.png` and `image-2.png` appeared in a deposit; they were byte-identical. Hash or size-compare anything the claim is about, or the check certifies a filename. The same session's integration suite showed the other face of it: a format test passed for months because its guard (`if result.get("drive_results")`) was never true, so it checked nothing. **Old tests often pin old bugs** — `rename_sheet(sheet_id=0)`, a seconds-less datetime, a fabricated `activity_id="act-123"` — so "the suite is green" says nothing about a path until someone has read what the tests assert. **For dependency surprises, test the latest upstream release before designing a workaround:** a 38-character reproduction plus `uv run --with pkg==X` across three versions settled the Beautiful Soup question in two minutes and replaced a code workaround with a version floor. **Essayeur subagents in this repo should run on a 1M-context model or be told to read one file per `git show`** — two Fable readers died of the inherited guidance corpus before reaching the code (`carte-docide`).
